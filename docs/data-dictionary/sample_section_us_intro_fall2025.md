---
notion-id: 3ced9fdd-1a1a-81f8-aa97-fe95d361af27
notion-url: https://app.notion.com/p/sample_section_us_intro_fall2025-3ced9fdd1a1a81f8aa97fe95d361af27
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `sample_section_us_intro_fall2025` data dictionary

- Relation kind: view
- Grain / key: Filtered master_section rows
- Pipeline stage: 4_merged_records.sql
- Direct upstream relations: `master_section`

| Column | Type | Upstream table | Derivation | Sample values | Description | NULL meaning |
|---|---|---|---|---|---|---|
| `section_id` | `varchar` | master_section | Inherited `master_section.section_id` via `SELECT *`. | 1001::BIO::101::001::2025-4 | Period-specific identifier for the distinct section offering. | Never NULL |
| `course_id` | `varchar` | master_section | Inherited `master_section.course_id` via `SELECT *`. | 1001::BIO::101 | Stable identifier for the institution-level course offering. | Missing segments are `UNKNOWN` |
| `period` | `varchar` | master_section | Inherited `master_section.period` via `SELECT *`. | Fall 2025 | Human-readable academic term label from the source. | Valid sortable period required |
| `period_sortable` | `varchar` | master_section | Inherited `master_section.period_sortable` via `SELECT *`. | 2025-4 | Sortable academic term code used for chronological ordering. | Never NULL; required by the sample filter. |
| `period_date` | `date` | master_section | Inherited `master_section.period_date` via `SELECT *`. | 2025-10-01 | Canonical starting date assigned to the academic term. | Not NULL in retained scope |
| `unit_id` | `bigint` | master_section | Inherited `master_section.unit_id` via `SELECT *`. | 1001 | IPEDS institution identifier used throughout the pipeline. | Missing institution ID |
| `state` | `varchar` | master_section | Inherited `master_section.state` via `SELECT *`. | CA | State or province code for the institution. | Never NULL; SQL `NOT IN ('CAN', '')` excludes NULL and the exact values `''` and `CAN` without trimming. |
| `control` | `varchar` | master_section | Inherited `master_section.control` via `SELECT *`. | unknown | Institution ownership and governance classification used for reporting. | No matching IPEDS institution |
| `level` | `varchar` | master_section | Inherited `master_section.level` via `SELECT *`. | unknown | IPEDS award-level classification for the institution. | No matching IPEDS institution |
| `size` | `varchar` | master_section | Inherited `master_section.size` via `SELECT *`. | unknown | IPEDS institutional enrollment-size classification used for reporting. | No matching IPEDS institution |
| `sector` | `varchar` | master_section | Inherited `master_section.sector` via `SELECT *`. | unknown | IPEDS sector classification for the institution. | No matching IPEDS institution |
| `institution_name` | `varchar` | master_section | Inherited `master_section.institution_name` via `SELECT *`. | Example University | Canonical institution name supplied by IPEDS. | No matching IPEDS institution |
| `institution_type` | `varchar` | master_section | Inherited `master_section.institution_type` via `SELECT *`. | unknown | Derived institution type used for reporting groups. | No matching institution/type |
| `enrollment_2024` | `integer` | master_section | Inherited `master_section.enrollment_2024` via `SELECT *`. | 42 | Total institutional enrollment reported to IPEDS for 2024. | No matching IPEDS value |
| `distance_enrollment_2024` | `integer` | master_section | Inherited `master_section.distance_enrollment_2024` via `SELECT *`. | 42 | IPEDS 2024 students enrolled in distance education. | No matching IPEDS value |
| `school` | `varchar` | master_section | Inherited `master_section.school` via `SELECT *`. | Example University | Institution or school name attached to the course. | No source value |
| `department` | `varchar` | master_section | Inherited `master_section.department` via `SELECT *`. | Biology | Academic department responsible for the course. | No source value |
| `course_number` | `varchar` | master_section | Inherited `master_section.course_number` via `SELECT *`. | 101 | Catalog number identifying the course within its department. | No source value |
| `section` | `varchar` | master_section | Inherited `master_section.section` via `SELECT *`. | 001 | Source code distinguishing sections of the same course. | No source value |
| `course_title` | `varchar` | master_section | Inherited `master_section.course_title` via `SELECT *`. | Introduction to Biology | Official title assigned to the course. | No source value |
| `course_level` | `varchar` | master_section | Inherited `master_section.course_level` via `SELECT *`. | Introductory or general undergraduate | Instructional level assigned to the course. | Never NULL; required by the sample filter. |
| `course_subject` | `varchar` | master_section | Inherited `master_section.course_subject` via `SELECT *`. | BIO | Subject area assigned to the course. | No source value |
| `material_count` | `bigint` | master_section | Inherited `master_section.material_count` via `SELECT *`. | 42 | Canonical course-material items within the section. | Never NULL/zero |
| `required_count` | `bigint` | master_section | Inherited `master_section.required_count` via `SELECT *`. | 42 | Required materials within the canonical section. | Never NULL; the sample filter requires `required_count >= 1`. |
| `optional_count` | `bigint` | master_section | Inherited `master_section.optional_count` via `SELECT *`. | 42 | Optional or recommended materials within the section. | Zero means none |
| `has_course_material_use` | `boolean` | master_section | Inherited `master_section.has_course_material_use` via `SELECT *`. | TRUE | Whether the section contains retained canonical materials. | Never false |
| `course_material_use_count` | `bigint` | master_section | Inherited `master_section.course_material_use_count` via `SELECT *`. | 42 | Canonical included materials within the retained section. | Never zero |
| `course_material_no_use_count` | `bigint` | master_section | Inherited `master_section.course_material_no_use_count` via `SELECT *`. | 42 | Excluded material rows co-occurring with retained sections. | Zero means none |
| `no_details_count` | `bigint` | master_section | Inherited `master_section.no_details_count` via `SELECT *`. | 42 | No-details placeholder rows co-occurring with retained sections. | Zero means marker absent |
| `no_materials_count` | `bigint` | master_section | Inherited `master_section.no_materials_count` via `SELECT *`. | 42 | No-materials placeholder rows co-occurring with retained sections. | Zero means marker absent |
| `is_canada` | `boolean` | master_section | Inherited `master_section.is_canada` via `SELECT *`. | TRUE | Whether the source row belongs to Canada. | False means no Canadian item |
| `is_supply` | `boolean` | master_section | Inherited `master_section.is_supply` via `SELECT *`. | TRUE | Whether the material is classified as a course supply. | False means no supply item |
| `supply_count` | `bigint` | master_section | Inherited `master_section.supply_count` via `SELECT *`. | 42 | Classified supply rows co-occurring with retained sections. | Zero means none |
| `is_oer` | `boolean` | master_section | Inherited `master_section.is_oer` via `SELECT *`. | TRUE | Whether the material is an open educational resource. | False means no classified OER |
| `is_ia` | `boolean` | master_section | Inherited `master_section.is_ia` via `SELECT *`. | TRUE | Whether the material uses inclusive access. | False means no classified IA |
| `oer_count` | `bigint` | master_section | Inherited `master_section.oer_count` via `SELECT *`. | 42 | Open-resource materials within the aggregation group. | Zero means none |
| `ia_count` | `bigint` | master_section | Inherited `master_section.ia_count` via `SELECT *`. | 42 | Inclusive-access materials within the aggregation group. | Zero means none |
| `publishers` | `varchar[]` | master_section | Inherited `master_section.publishers` via `SELECT *`. | ['alpha', 'beta'] | Distinct publishers represented by the section materials. | No item publisher |
| `required_publishers` | `varchar[]` | master_section | Inherited `master_section.required_publishers` via `SELECT *`. | ['alpha', 'beta'] | Distinct publishers of required materials within the section. | No required publisher |
| `required_publisher_count` | `bigint` | master_section | Inherited `master_section.required_publisher_count` via `SELECT *`. | 42 | Distinct publishers of required materials within the section. | Zero means none |
| `optional_publisher_count` | `bigint` | master_section | Inherited `master_section.optional_publisher_count` via `SELECT *`. | 42 | Distinct publishers of optional materials within the section. | Zero means none |
| `enrollments` | `integer` | master_section | Inherited `master_section.enrollments` via `SELECT *`. | 42 | Enrollment reported directly for the course section. | Unavailable |
| `seats_taken` | `integer` | master_section | Inherited `master_section.seats_taken` via `SELECT *`. | 42 | Occupied seats reported for the course section. | Unavailable |
| `has_isbn` | `boolean` | master_section | Inherited `master_section.has_isbn` via `SELECT *`. | TRUE | Whether the material has a non-NULL ISBN. | True under current Use contract |
| `has_formattype` | `boolean` | master_section | Inherited `master_section.has_formattype` via `SELECT *`. | TRUE | Whether the material has a nonblank FormatType classification. | False means no nonblank FormatType |
| `isbn_count` | `bigint` | master_section | Inherited `master_section.isbn_count` via `SELECT *`. | 42 | ISBN-bearing materials within the aggregation group. | Never zero |
| `classified_count` | `bigint` | master_section | Inherited `master_section.classified_count` via `SELECT *`. | 42 | Materials having a nonblank FormatType classification. | Zero means none |
| `has_enrollment` | `boolean` | master_section | Inherited `master_section.has_enrollment` via `SELECT *`. | TRUE | Whether usable enrollment information is available. | Never NULL |
| `has_enrollment_sibling` | `boolean` | master_section | Inherited `master_section.has_enrollment_sibling` via `SELECT *`. | TRUE | Whether a sibling section reports enrollment. | Never NULL |
| `has_enrollment_own_seats` | `boolean` | master_section | Inherited `master_section.has_enrollment_own_seats` via `SELECT *`. | TRUE | Whether the section reports usable occupied seats. | Never NULL |
| `has_enrollment_sibling_seats` | `boolean` | master_section | Inherited `master_section.has_enrollment_sibling_seats` via `SELECT *`. | TRUE | Whether a sibling section reports usable occupied seats. | Never NULL |
| `enrollment_assigned` | `integer` | master_section | Inherited `master_section.enrollment_assigned` via `SELECT *`. | 42 | Best available section enrollment from the assignment ladder. | No rung; source is `none` |
| `enrollment_source` | `varchar` | master_section | Inherited `master_section.enrollment_source` via `SELECT *`. | own | Assignment-ladder rung that supplied the section enrollment. | Never NULL; values `own`, `own_seats`, `sibling_enroll`, `sibling_seats`, `class_median`, `level_median`, `none` |
| `is_required_direct` | `boolean` | master_section | Inherited `master_section.is_required_direct` via `SELECT *`. | TRUE | Whether direct required evidence is present at the relation grain. | False means no nonsupply literal-required item in the section |
| `required_price_min` | `decimal(38,2)` | master_section | Inherited `master_section.required_price_min` via `SELECT *`. | 12.34 | Lower all-offer price bound for required materials. | No required item, or every required item price is NULL |
| `required_price_avg` | `double` | master_section | Inherited `master_section.required_price_avg` via `SELECT *`. | 12.34 | Legacy-named midrange of required price bounds. | Either bound is NULL |
| `required_price_max` | `decimal(38,2)` | master_section | Inherited `master_section.required_price_max` via `SELECT *`. | 12.34 | Upper all-offer price bound for required materials. | No required item, or every required item price is NULL |
| `required_price_buy_min` | `decimal(38,2)` | master_section | Inherited `master_section.required_price_buy_min` via `SELECT *`. | 12.34 | Lower buy-only price bound for required materials. | No required item, or every required buy price is NULL |
| `required_price_buy_max` | `decimal(38,2)` | master_section | Inherited `master_section.required_price_buy_max` via `SELECT *`. | 12.34 | Upper buy-only price bound for required materials. | No required item, or every required buy price is NULL |
| `all_price_min` | `decimal(38,2)` | master_section | Inherited `master_section.all_price_min` via `SELECT *`. | 12.34 | Lower all-offer price bound for all-item materials. | Every item price is NULL |
| `all_price_avg` | `double` | master_section | Inherited `master_section.all_price_avg` via `SELECT *`. | 12.34 | Legacy-named midrange of all-item price bounds. | Either bound is NULL |
| `all_price_max` | `decimal(38,2)` | master_section | Inherited `master_section.all_price_max` via `SELECT *`. | 12.34 | Upper all-offer price bound for all-item materials. | Every item price is NULL |
| `all_price_buy_min` | `decimal(38,2)` | master_section | Inherited `master_section.all_price_buy_min` via `SELECT *`. | 12.34 | Lower buy-only price bound for all-item materials. | Every item buy price is NULL |
| `all_price_buy_max` | `decimal(38,2)` | master_section | Inherited `master_section.all_price_buy_max` via `SELECT *`. | 12.34 | Upper buy-only price bound for all-item materials. | Every item buy price is NULL |
| `required_priced_count` | `bigint` | master_section | Inherited `master_section.required_priced_count` via `SELECT *`. | 42 | Required materials having at least one valid price. | Zero means none |
| `optional_priced_count` | `bigint` | master_section | Inherited `master_section.optional_priced_count` via `SELECT *`. | 42 | Optional materials having at least one valid price. | Zero means none |
| `bookstore_url` | `varchar` | master_section | Inherited `master_section.bookstore_url` via `SELECT *`. | https://bookstore.example.edu/item/123 | Bookstore page providing the observed material prices. | No nonblank item URL |
