---
notion-id: 3ced9fdd-1a1a-81d9-827b-fa1ebff3c328
notion-url: https://app.notion.com/p/section_cost-3ced9fdd1a1a81d9827bfa1ebff3c328
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `section_cost` data dictionary

- Relation kind: table
- Grain / key: One material-bearing period × section
- Pipeline stage: EDA records / 4_merged_records.sql
- Direct upstream relations: `material_costs`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `section_id` | `varchar` | `course_id::section-code::period_sortable` composite | Group key / constant value from `material_costs.section_id`. | Period-specific identifier for the distinct section offering. |
| `course_id` | `varchar` | `unit_id::department-code::course-number` composite | Group key / constant value from `material_costs.course_id`. | Stable identifier for the institution-level course offering. |
| `period_sortable` | `varchar` | `YYYY-N`; 1=Winter, 2=Spring, 3=Summer, 4=Fall | Group key / constant value from `material_costs.period_sortable`. | Sortable academic term code used for chronological ordering. |
| `required_cost_total_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_min` for required canonical ISBN items; total scope. | Lower total cost bound for required materials. |
| `required_cost_total_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_max` for required canonical ISBN items; total scope. | Upper total cost bound for required materials. |
| `optional_cost_total_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_min` for optional canonical ISBN items; total scope. | Lower total cost bound for optional materials. |
| `optional_cost_total_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_max` for optional canonical ISBN items; total scope. | Upper total cost bound for optional materials. |
| `required_cost_owned_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_buy_min` for required canonical ISBN items; owned scope. | Lower buy-only cost bound for required materials. |
| `required_cost_owned_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_buy_max` for required canonical ISBN items; owned scope. | Upper buy-only cost bound for required materials. |
| `optional_cost_owned_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_buy_min` for optional canonical ISBN items; owned scope. | Lower buy-only cost bound for optional materials. |
| `optional_cost_owned_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | SUM of distinct `price_buy_max` for optional canonical ISBN items; owned scope. | Upper buy-only cost bound for optional materials. |
| `required_priced_count` | `bigint` | Non-negative whole-number count | Count of required canonical ISBN items with non-NULL `price_min`. | Required materials having at least one valid price. |
| `optional_priced_count` | `bigint` | Non-negative whole-number count | Count of optional canonical ISBN items with non-NULL `price_min`. | Optional materials having at least one valid price. |
