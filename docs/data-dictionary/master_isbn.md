---
notion-id: 3ced9fdd-1a1a-812a-8738-edd614803645
notion-url: https://app.notion.com/p/master_isbn-3ced9fdd1a1a812a8738edd614803645
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `master_isbn` data dictionary

- Relation kind: table
- Grain / key: One period × non-NULL ISBN
- Pipeline stage: Release model / models/master_isbn.sql
- Direct upstream relations: `material_costs`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `period_sortable` | `varchar` | YYYY-N; part of canonical key | Group key from `material_costs.period_sortable`. | Sortable academic term code used for chronological ordering. |
| `isbn13` | `bigint` | 13-digit numeric ISBN or numeric source identifier; NULL when absent | Group key from `material_costs.isbn13`. | Book or material identifier used for cross-source matching. |
| `book_title` | `varchar` | Material title text; source punctuation and casing retained | `ANY_VALUE(material_costs.isbn_book_title)`; term×ISBN metadata is canonical upstream. | Canonical title selected for the grouped ISBN. |
| `author` | `varchar` | Person or organization name; source punctuation and casing retained | `ANY_VALUE(material_costs.isbn_author)`; term×ISBN metadata is canonical upstream. | Author credited for the adopted course material. |
| `publisher` | `varchar` | Publisher-name text; original spelling and casing retained | `ANY_VALUE(material_costs.isbn_publisher)`; term×ISBN metadata is canonical upstream. | Publisher credited for the adopted course material. |
| `is_oer` | `boolean` | Definitive BOOL_OR across catalog rows | `COALESCE(BOOL_OR(material_costs.is_oer), FALSE)`. | Whether the material is an open educational resource. |
| `is_ia` | `boolean` | Definitive BOOL_OR across catalog rows | `COALESCE(BOOL_OR(material_costs.is_ia), FALSE)`. | Whether the material uses inclusive access. |
| `is_supply` | `boolean` | Always false in the canonical Use population | Constant FALSE: supplies are excluded from the canonical Use population. | Whether the material is classified as a course supply. |
| `unit_id_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT unit_id)`. | Distinct institutions using the ISBN during the term. |
| `section_id_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)`. | Distinct sections using the ISBN during the term. |
| `course_id_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT course_id)`. | Distinct courses using the ISBN during the term. |
| `has_enrollment` | `boolean` | TRUE or FALSE | `COALESCE(BOOL_OR(enrollment_assigned IS NOT NULL), FALSE)`. | Whether usable enrollment information is available. |
| `enroll_cnt` | `bigint` | Distinct sections with enrollment_assigned | `COUNT(DISTINCT section_id) FILTER (WHERE enrollment_assigned IS NOT NULL)`. | ISBN-using sections having an assigned enrollment value. |
| `enroll_tot` | `hugeint` | Sum of assigned enrollment; negative source values may propagate | `COALESCE(SUM(enrollment_assigned), 0)` across ISBN occurrences. | Assigned enrollment summed across all ISBN-using sections. |
| `title_variant_count` | `bigint` | Non-negative whole-number count | `ANY_VALUE(material_costs.isbn_title_variant_count)`. | Distinct title variants found within grouped catalog rows. |
| `author_variant_count` | `bigint` | Non-negative whole-number count | `ANY_VALUE(material_costs.isbn_author_variant_count)`. | Distinct author variants found within grouped catalog rows. |
| `publisher_variant_count` | `bigint` | Non-negative whole-number count | `ANY_VALUE(material_costs.isbn_publisher_variant_count)`. | Distinct publisher variants found within grouped catalog rows. |
| `metadata_conflict` | `boolean` | True when any metadata variant count exceeds one | True when any title/author/publisher variant count exceeds one. | Flags conflicting canonical bibliographic values for the ISBN. |
| `price_buy_new_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_physical IS NOT NULL)`. | Sections offering new physical purchase prices. |
| `price_buy_new_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_digital IS NOT NULL)`. | Sections offering new digital purchase prices. |
| `price_buy_new_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_na IS NOT NULL)`. | Sections offering new unspecified-format purchase prices. |
| `price_buy_used_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_physical IS NOT NULL)`. | Sections offering used physical purchase prices. |
| `price_buy_used_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_digital IS NOT NULL)`. | Sections offering used digital purchase prices. |
| `price_buy_used_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_na IS NOT NULL)`. | Sections offering used unspecified-format purchase prices. |
| `price_buy_na_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_physical IS NOT NULL)`. | Sections offering unspecified-condition physical purchase prices. |
| `price_buy_na_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_digital IS NOT NULL)`. | Sections offering unspecified-condition digital purchase prices. |
| `price_buy_na_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_na IS NOT NULL)`. | Sections offering unspecified-condition unspecified-format purchase prices. |
| `price_rental_new_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_physical IS NOT NULL)`. | Sections offering new physical rental prices. |
| `price_rental_new_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_digital IS NOT NULL)`. | Sections offering new digital rental prices. |
| `price_rental_new_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_na IS NOT NULL)`. | Sections offering new unspecified-format rental prices. |
| `price_rental_used_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_physical IS NOT NULL)`. | Sections offering used physical rental prices. |
| `price_rental_used_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_digital IS NOT NULL)`. | Sections offering used digital rental prices. |
| `price_rental_used_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_na IS NOT NULL)`. | Sections offering used unspecified-format rental prices. |
| `price_rental_na_physical_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_physical IS NOT NULL)`. | Sections offering unspecified-condition physical rental prices. |
| `price_rental_na_digital_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_digital IS NOT NULL)`. | Sections offering unspecified-condition digital rental prices. |
| `price_rental_na_na_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_na IS NOT NULL)`. | Sections offering unspecified-condition unspecified-format rental prices. |
| `institution_type_2_year_private_profit_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Priavte Profit` (exact source literal, including source spelling). | ISBN occurrences from two-year private for-profit institutions. |
| `institution_type_2_year_private_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Private` (exact source literal, including source spelling). | ISBN occurrences from two-year private institutions. |
| `institution_type_2_year_public_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Public` (exact source literal, including source spelling). | ISBN occurrences from two-year public institutions. |
| `institution_type_4_year_private_profit_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Priavte Profit` (exact source literal, including source spelling). | ISBN occurrences from four-year private for-profit institutions. |
| `institution_type_4_year_private_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Private` (exact source literal, including source spelling). | ISBN occurrences from four-year private institutions. |
| `institution_type_4_year_public_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Public` (exact source literal, including source spelling). | ISBN occurrences from four-year public institutions. |
| `institution_type_under_2_year_public_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `Under 2 Year Public` (exact source literal, including source spelling). | ISBN occurrences from under-two-year public institutions. |
| `institution_type_unknown_count` | `bigint` | Non-negative whole-number count | `COUNT(DISTINCT section_id)` FILTERed where institution_type is NULL or blank. | ISBN occurrences from unknown-type institutions. |
