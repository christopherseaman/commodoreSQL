---
notion-id: 3ced9fdd-1a1a-812a-8738-edd614803645
notion-url: https://app.notion.com/p/master_isbn-3ced9fdd1a1a812a8738edd614803645
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `master_isbn` data dictionary

- Relation kind: table
- Grain / key: One period × non-NULL ISBN
- Pipeline stage: Release model / models/master_isbn.sql
- Direct upstream relations: `master_material`

| Column | Type | Upstream table | Derivation | Sample values | Description | NULL meaning |
|---|---|---|---|---|---|---|
| `period_sortable` | `varchar` | master_material | `master_material.period_sortable` group key. | 2025-4 | Sortable academic term code used for chronological ordering. | Not produced. |
| `isbn13` | `bigint` | master_material | `master_material.isbn13` group key. | 9780000000002 | Book or material identifier used for cross-source matching. | Not produced. |
| `book_title` | `varchar` | master_material | `ANY_VALUE(master_material.isbn_book_title)`; term×ISBN metadata is canonical upstream. | Introduction to Biology | Canonical title selected for the grouped ISBN. | No nonmissing canonical ISBN metadata. |
| `author` | `varchar` | master_material | `ANY_VALUE(master_material.isbn_author)`; term×ISBN metadata is canonical upstream. | A. Example | Author credited for the adopted course material. | No nonmissing canonical ISBN metadata. |
| `publisher` | `varchar` | master_material | `ANY_VALUE(master_material.isbn_publisher)`; term×ISBN metadata is canonical upstream. | Example Press | Publisher credited for the adopted course material. | No nonmissing canonical ISBN metadata. |
| `is_oer` | `boolean` | master_material | `COALESCE(BOOL_OR(master_material.is_oer), FALSE)`. | TRUE | Whether the material is an open educational resource. | Never NULL; false means no canonical ISBN row has the classification. |
| `is_ia` | `boolean` | master_material | `COALESCE(BOOL_OR(master_material.is_ia), FALSE)`. | TRUE | Whether the material uses inclusive access. | Never NULL; false means no canonical ISBN row has the classification. |
| `is_supply` | `boolean` | master_material | Constant FALSE: supplies are excluded from the canonical Use population. | FALSE | Whether the material is classified as a course supply. | Never NULL. |
| `unit_id_count` | `bigint` | master_material | `COUNT(DISTINCT unit_id)`. | 42 | Distinct institutions using the ISBN during the term. | Never NULL; zero means all values of that key are missing. |
| `section_id_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)`. | 42 | Distinct sections using the ISBN during the term. | Never NULL; zero means all values of that key are missing. |
| `course_id_count` | `bigint` | master_material | `COUNT(DISTINCT course_id)`. | 42 | Distinct courses using the ISBN during the term. | Never NULL; zero means all values of that key are missing. |
| `has_enrollment` | `boolean` | master_material | `COALESCE(BOOL_OR(enrollment_assigned IS NOT NULL), FALSE)`. | TRUE | Whether usable enrollment information is available. | Never NULL; false means no occurrence has assigned enrollment. |
| `enroll_cnt` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE enrollment_assigned IS NOT NULL)`. | 42 | ISBN-using sections having an assigned enrollment value. | Never NULL; zero means none has assigned enrollment. |
| `enroll_tot` | `hugeint` | master_material | `COALESCE(SUM(enrollment_assigned), 0)` across ISBN occurrences. | 42 | Assigned enrollment summed across all ISBN-using sections. | Never NULL; zero means no assigned enrollment or assigned total zero. |
| `title_variant_count` | `bigint` | master_material | `ANY_VALUE(master_material.isbn_title_variant_count)`. | 42 | Distinct title variants found within grouped catalog rows. | Not expected; upstream count is non-NULL. |
| `author_variant_count` | `bigint` | master_material | `ANY_VALUE(master_material.isbn_author_variant_count)`. | 42 | Distinct author variants found within grouped catalog rows. | Not expected; upstream count is non-NULL. |
| `publisher_variant_count` | `bigint` | master_material | `ANY_VALUE(master_material.isbn_publisher_variant_count)`. | 42 | Distinct publisher variants found within grouped catalog rows. | Not expected; upstream count is non-NULL. |
| `metadata_conflict` | `boolean` | master_material | True when any title/author/publisher variant count exceeds one. | TRUE | Flags conflicting canonical bibliographic values for the ISBN. | Never NULL; false means no detected canonical metadata conflict. |
| `required_price_min` | `decimal(38,2)` | master_material | `SUM(master_material.price_min) FILTER (WHERE is_required_inferred)`. | 12.34 | Lower all-offer price bound for required materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `required_price_max` | `decimal(38,2)` | master_material | `SUM(master_material.price_max) FILTER (WHERE is_required_inferred)`. | 12.34 | Upper all-offer price bound for required materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `required_price_buy_min` | `decimal(38,2)` | master_material | `SUM(master_material.price_buy_min) FILTER (WHERE is_required_inferred)`. | 12.34 | Lower buy-only price bound for required materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `required_price_buy_max` | `decimal(38,2)` | master_material | `SUM(master_material.price_buy_max) FILTER (WHERE is_required_inferred)`. | 12.34 | Upper buy-only price bound for required materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `all_price_min` | `decimal(38,2)` | master_material | `SUM(master_material.price_min)`. | 12.34 | Lower all-offer price bound for all-item materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `all_price_max` | `decimal(38,2)` | master_material | `SUM(master_material.price_max)`. | 12.34 | Upper all-offer price bound for all-item materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `all_price_buy_min` | `decimal(38,2)` | master_material | `SUM(master_material.price_buy_min)`. | 12.34 | Lower buy-only price bound for all-item materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `all_price_buy_max` | `decimal(38,2)` | master_material | `SUM(master_material.price_buy_max)`. | 12.34 | Upper buy-only price bound for all-item materials. | All qualifying occurrence values are missing; individual NULLs are ignored and genuine zero is retained. |
| `price_buy_new_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_physical IS NOT NULL)`. | 42 | Sections offering new physical purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_new_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_digital IS NOT NULL)`. | 42 | Sections offering new digital purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_new_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_new_na IS NOT NULL)`. | 42 | Sections offering new unspecified-format purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_used_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_physical IS NOT NULL)`. | 42 | Sections offering used physical purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_used_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_digital IS NOT NULL)`. | 42 | Sections offering used digital purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_used_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_used_na IS NOT NULL)`. | 42 | Sections offering used unspecified-format purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_na_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_physical IS NOT NULL)`. | 42 | Sections offering unspecified-condition physical purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_na_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_digital IS NOT NULL)`. | 42 | Sections offering unspecified-condition digital purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_buy_na_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_buy_na_na IS NOT NULL)`. | 42 | Sections offering unspecified-condition unspecified-format purchase prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_new_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_physical IS NOT NULL)`. | 42 | Sections offering new physical rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_new_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_digital IS NOT NULL)`. | 42 | Sections offering new digital rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_new_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_new_na IS NOT NULL)`. | 42 | Sections offering new unspecified-format rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_used_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_physical IS NOT NULL)`. | 42 | Sections offering used physical rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_used_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_digital IS NOT NULL)`. | 42 | Sections offering used digital rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_used_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_used_na IS NOT NULL)`. | 42 | Sections offering used unspecified-format rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_na_physical_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_physical IS NOT NULL)`. | 42 | Sections offering unspecified-condition physical rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_na_digital_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_digital IS NOT NULL)`. | 42 | Sections offering unspecified-condition digital rental prices. | Never NULL; zero means no section has that price cell. |
| `price_rental_na_na_count` | `bigint` | master_material | `COUNT(DISTINCT section_id) FILTER (WHERE price_rental_na_na IS NOT NULL)`. | 42 | Sections offering unspecified-condition unspecified-format rental prices. | Never NULL; zero means no section has that price cell. |
| `institution_type_2_year_private_profit_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Priavte Profit` (exact source literal, including source spelling). | 42 | ISBN occurrences from two-year private for-profit institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_2_year_private_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Private` (exact source literal, including source spelling). | 42 | ISBN occurrences from two-year private institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_2_year_public_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `2 Year Public` (exact source literal, including source spelling). | 42 | ISBN occurrences from two-year public institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_4_year_private_profit_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Priavte Profit` (exact source literal, including source spelling). | 42 | ISBN occurrences from four-year private for-profit institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_4_year_private_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Private` (exact source literal, including source spelling). | 42 | ISBN occurrences from four-year private institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_4_year_public_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `4 Year Public` (exact source literal, including source spelling). | 42 | ISBN occurrences from four-year public institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_under_2_year_public_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type = `Under 2 Year Public` (exact source literal, including source spelling). | 42 | ISBN occurrences from under-two-year public institutions. | Never NULL; zero means no qualifying section. |
| `institution_type_unknown_count` | `bigint` | master_material | `COUNT(DISTINCT section_id)` FILTERed where institution_type is NULL or blank. | 42 | ISBN occurrences from unknown-type institutions. | Never NULL; zero means no qualifying section. |
