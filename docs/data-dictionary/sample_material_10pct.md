---
notion-id: 3d2d9fdd-1a1a-81f2-9eb7-f380f1ce188f
notion-url: https://app.notion.com/p/sample_material_10pct-3d2d9fdd1a1a81f29eb7f380f1ce188f
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `sample_material_10pct` data dictionary

- Relation kind: table
- Grain / key: One sampled Material Costs period × section × ISBN item
- Pipeline stage: Sampling / models/sample_material_10pct.sql
- Direct upstream relations: `master_material`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `isbn13` | `bigint` | 13-digit numeric ISBN or numeric source identifier; NULL when absent | `master_material.isbn13` retained after the deterministic section-hash filter. | Book or material identifier used for cross-source matching. |
| `book_title` | `varchar` | Material title; source punctuation/casing retained | `master_material.book_title` retained after the deterministic section-hash filter. | Canonical title selected for the grouped ISBN. |
| `author` | `varchar` | Person/organization name; source punctuation/casing retained | `master_material.author` retained after the deterministic section-hash filter. | Author credited for the adopted course material. |
| `publisher` | `varchar` | Publisher name; original spelling/casing retained | `master_material.publisher` retained after the deterministic section-hash filter. | Publisher credited for the adopted course material. |
| `imprint` | `varchar` | Publishing imprint; original spelling/casing retained | `master_material.imprint` retained after the deterministic section-hash filter. | Publishing imprint named for the adopted material. |
| `book_format` | `varchar` | Source material format; pricing uses `physical`, `digital`, or unavailable format | `master_material.book_format` retained after the deterministic section-hash filter. | Physical or digital format of the material. |
| `format_type` | `varchar` | Catalog FormatType label; original spelling retained | `master_material.format_type` retained after the deterministic section-hash filter. | Catalog classification label for the material format. |
| `book_status` | `varchar` | Lowercase `required`, `recommended`, `option`, or missing status | `master_material.book_status` retained after the deterministic section-hash filter. | Adoption priority assigned to the course material. |
| `isbn_book_title` | `varchar` | Canonical title text selected for the term and ISBN | `master_material.isbn_book_title` retained after the deterministic section-hash filter. | Canonical title selected for the term-level ISBN. |
| `isbn_author` | `varchar` | Canonical author text selected for the term and ISBN | `master_material.isbn_author` retained after the deterministic section-hash filter. | Canonical author selected for the term-level ISBN. |
| `isbn_publisher` | `varchar` | Canonical publisher text selected for the term and ISBN | `master_material.isbn_publisher` retained after the deterministic section-hash filter. | Canonical publisher selected for the term-level ISBN. |
| `isbn_title_variant_count` | `bigint` | Non-negative whole-number count | `master_material.isbn_title_variant_count` retained after the deterministic section-hash filter. | Distinct ISBN title variants found within grouped catalog rows. |
| `isbn_author_variant_count` | `bigint` | Non-negative whole-number count | `master_material.isbn_author_variant_count` retained after the deterministic section-hash filter. | Distinct ISBN author variants found within grouped catalog rows. |
| `isbn_publisher_variant_count` | `bigint` | Non-negative whole-number count | `master_material.isbn_publisher_variant_count` retained after the deterministic section-hash filter. | Distinct ISBN publisher variants found within grouped catalog rows. |
| `unit_id` | `bigint` | IPEDS institution identifier | `master_material.unit_id` retained after the deterministic section-hash filter. | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution name; source spelling/casing retained | `master_material.school` retained after the deterministic section-hash filter. | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | `master_material.state` retained after the deterministic section-hash filter. | State or province code for the institution. |
| `dept_code` | `varchar` | Department code, such as `BIOL` | `master_material.dept_code` retained after the deterministic section-hash filter. | Short code identifying the academic department. |
| `department` | `varchar` | Academic department, such as `Biology` | `master_material.department` retained after the deterministic section-hash filter. | Academic department responsible for the course. |
| `dept_description` | `varchar` | Expanded source description of the academic department | `master_material.dept_description` retained after the deterministic section-hash filter. | Expanded description of the academic department. |
| `course_number` | `varchar` | Source course number; zeros/suffixes retained | `master_material.course_number` retained after the deterministic section-hash filter. | Catalog number identifying the course within its department. |
| `section` | `varchar` | Source section code; zeros/punctuation retained | `master_material.section` retained after the deterministic section-hash filter. | Source code distinguishing sections of the same course. |
| `course_title` | `varchar` | Course title; source punctuation/casing retained | `master_material.course_title` retained after the deterministic section-hash filter. | Official title assigned to the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | `master_material.course_level` retained after the deterministic section-hash filter. | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject, such as `Biology` | `master_material.course_subject` retained after the deterministic section-hash filter. | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | `master_material.period` retained after the deterministic section-hash filter. | Human-readable academic term label from the source. |
| `enrollments` | `integer` | Reported student count; source noise can include negative values | `master_material.enrollments` retained after the deterministic section-hash filter. | Enrollment reported directly for the course section. |
| `seats_taken` | `integer` | Reported occupied seats; `9999` is the source sentinel | `master_material.seats_taken` retained after the deterministic section-hash filter. | Occupied seats reported for the course section. |
| `instructor` | `varchar` | Instructor name; source punctuation/casing retained | `master_material.instructor` retained after the deterministic section-hash filter. | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given name; source punctuation/casing retained | `master_material.first_name` retained after the deterministic section-hash filter. | Given name of the course instructor. |
| `last_name` | `varchar` | Family name; source punctuation/casing retained | `master_material.last_name` retained after the deterministic section-hash filter. | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | `master_material.email` retained after the deterministic section-hash filter. | Normalized instructor email used for contact and matching. |
| `course_id` | `varchar` | `unit_id::department-code::course-number` composite | `master_material.course_id` retained after the deterministic section-hash filter. | Stable identifier for the institution-level course offering. |
| `section_id` | `varchar` | period-specific section key | `master_material.section_id` retained after the deterministic section-hash filter. | Period-specific identifier for the distinct section offering. |
| `period_sortable` | `varchar` | YYYY-N | `master_material.period_sortable` retained after the deterministic section-hash filter. | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | `master_material.period_date` retained after the deterministic section-hash filter. | Canonical starting date assigned to the academic term. |
| `is_oer` | `boolean` | TRUE or FALSE | `master_material.is_oer` retained after the deterministic section-hash filter. | Whether the material is an open educational resource. |
| `oer_category` | `varchar` | Category such as `book_oer`, `pure_oer`, or `non_oer` | `master_material.oer_category` retained after the deterministic section-hash filter. | Open-resource category assigned to the material format. |
| `is_ia` | `boolean` | TRUE or FALSE | `master_material.is_ia` retained after the deterministic section-hash filter. | Whether the material uses inclusive access. |
| `ia_category` | `varchar` | Category such as `book_ia`, `ebook_ia`, or `non_ia` | `master_material.ia_category` retained after the deterministic section-hash filter. | Inclusive-access category assigned to the material format. |
| `is_supply` | `boolean` | TRUE or FALSE | `master_material.is_supply` retained after the deterministic section-hash filter. | Whether the material is classified as a course supply. |
| `supply_category` | `varchar` | Supply category label selected by keyword rules | `master_material.supply_category` retained after the deterministic section-hash filter. | Supply classification assigned from matched title keywords. |
| `institution_name` | `varchar` | Official IPEDS institution-name text | `master_material.institution_name` retained after the deterministic section-hash filter. | Canonical institution name supplied by IPEDS. |
| `sector` | `varchar` | IPEDS sector descriptor, such as `Public, 4-year or above` | `master_material.sector` retained after the deterministic section-hash filter. | IPEDS sector classification for the institution. |
| `level` | `varchar` | IPEDS label such as `Four or more years` | `master_material.level` retained after the deterministic section-hash filter. | IPEDS award-level classification for the institution. |
| `control` | `varchar` | IPEDS label such as `Public` or `Private not-for-profit` | `master_material.control` retained after the deterministic section-hash filter. | Institution ownership and governance classification used for reporting. |
| `size` | `varchar` | IPEDS size-band label, such as `20,000 and above` | `master_material.size` retained after the deterministic section-hash filter. | IPEDS institutional enrollment-size classification used for reporting. |
| `enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | `master_material.enrollment_2024` retained after the deterministic section-hash filter. | Total institutional enrollment reported to IPEDS for 2024. |
| `distance_enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | `master_material.distance_enrollment_2024` retained after the deterministic section-hash filter. | IPEDS 2024 students enrolled in distance education. |
| `institution_type` | `varchar` | Derived institution-type category label | `master_material.institution_type` retained after the deterministic section-hash filter. | Derived institution type used for reporting groups. |
| `panel_response_year` | `varchar` | Campaign label shaped `OER_YYYY`, such as `OER_2025` | `master_material.panel_response_year` retained after the deterministic section-hash filter. | Latest recorded panel response campaign label. |
| `is_opted_out` | `boolean` | TRUE or FALSE | `master_material.is_opted_out` retained after the deterministic section-hash filter. | Whether the contact appears in the opt-out list. |
| `opt_out_source` | `varchar` | BVA source-system label for the opt-out entry | `master_material.opt_out_source` retained after the deterministic section-hash filter. | BVA source label explaining the opt-out record. |
| `is_required_inferred` | `boolean` | TRUE or FALSE | `master_material.is_required_inferred` retained after the deterministic section-hash filter. | Whether the material is treated as required after fallback. |
| `is_post_2024` | `boolean` | TRUE or FALSE | `master_material.is_post_2024` retained after the deterministic section-hash filter. | Whether the academic term begins during 2024 or later. |
| `has_isbn` | `boolean` | TRUE or FALSE | `master_material.has_isbn` retained after the deterministic section-hash filter. | Whether the material has a non-NULL ISBN. |
| `has_formattype` | `boolean` | TRUE or FALSE | `master_material.has_formattype` retained after the deterministic section-hash filter. | Whether the material has a nonblank FormatType classification. |
| `has_enrollment` | `boolean` | TRUE or FALSE | `master_material.has_enrollment` retained after the deterministic section-hash filter. | Whether usable enrollment information is available. |
| `has_enrollment_own_seats` | `boolean` | TRUE or FALSE | `master_material.has_enrollment_own_seats` retained after the deterministic section-hash filter. | Whether the section reports usable occupied seats. |
| `no_details` | `boolean` | TRUE or FALSE | `master_material.no_details` retained after the deterministic section-hash filter. | Whether the title carries the no-details placeholder. |
| `no_materials` | `boolean` | TRUE or FALSE | `master_material.no_materials` retained after the deterministic section-hash filter. | Whether the row explicitly indicates no course materials. |
| `is_canada` | `boolean` | TRUE or FALSE | `master_material.is_canada` retained after the deterministic section-hash filter. | Whether the source row belongs to Canada. |
| `is_course_material_use` | `boolean` | TRUE or FALSE | `master_material.is_course_material_use` retained after the deterministic section-hash filter. | Whether the material belongs to the analysis population. |
| `is_course_material_no_use` | `boolean` | TRUE or FALSE | `master_material.is_course_material_no_use` retained after the deterministic section-hash filter. | Whether the material belongs to the excluded population. |
| `has_enrollment_sibling` | `boolean` | TRUE or FALSE | `master_material.has_enrollment_sibling` retained after the deterministic section-hash filter. | Whether a sibling section reports enrollment. |
| `has_enrollment_sibling_seats` | `boolean` | TRUE or FALSE | `master_material.has_enrollment_sibling_seats` retained after the deterministic section-hash filter. | Whether a sibling section reports usable occupied seats. |
| `enrollment_assigned` | `integer` | Rounded student count from the ladder; raw negatives can propagate | `master_material.enrollment_assigned` retained after the deterministic section-hash filter. | Best available section enrollment from the assignment ladder. |
| `enrollment_source` | `varchar` | `own`, `own_seats`, `sibling_enroll`, `sibling_seats`, `class_median`, `level_median`, or `none` | `master_material.enrollment_source` retained after the deterministic section-hash filter. | Assignment-ladder rung that supplied the section enrollment. |
| `is_required_direct` | `boolean` | TRUE or FALSE | `master_material.is_required_direct` retained after the deterministic section-hash filter. | Whether direct required evidence is present at the relation grain. |
| `is_optional_or_recommended_direct` | `boolean` | TRUE or FALSE | `master_material.is_optional_or_recommended_direct` retained after the deterministic section-hash filter. | Whether grouped rows include optional or recommended direct-status evidence. |
| `source_row_count` | `bigint` | Non-negative whole-number count | `master_material.source_row_count` retained after the deterministic section-hash filter. | Catalog rows collapsed into the canonical material item. |
| `title_variant_count` | `bigint` | Non-negative whole-number count | `master_material.title_variant_count` retained after the deterministic section-hash filter. | Distinct title variants found within grouped catalog rows. |
| `author_variant_count` | `bigint` | Non-negative whole-number count | `master_material.author_variant_count` retained after the deterministic section-hash filter. | Distinct author variants found within grouped catalog rows. |
| `publisher_variant_count` | `bigint` | Non-negative whole-number count | `master_material.publisher_variant_count` retained after the deterministic section-hash filter. | Distinct publisher variants found within grouped catalog rows. |
| `imprint_variant_count` | `bigint` | Non-negative whole-number count | `master_material.imprint_variant_count` retained after the deterministic section-hash filter. | Distinct imprint variants found within grouped catalog rows. |
| `book_format_variant_count` | `bigint` | Non-negative whole-number count | `master_material.book_format_variant_count` retained after the deterministic section-hash filter. | Distinct book format variants found within grouped catalog rows. |
| `format_type_variant_count` | `bigint` | Non-negative whole-number count | `master_material.format_type_variant_count` retained after the deterministic section-hash filter. | Distinct FormatType variants found within grouped catalog rows. |
| `book_status_variant_count` | `bigint` | Non-negative whole-number count | `master_material.book_status_variant_count` retained after the deterministic section-hash filter. | Distinct book status variants found within grouped catalog rows. |
| `catalog_metadata_conflict` | `boolean` | TRUE or FALSE | `master_material.catalog_metadata_conflict` retained after the deterministic section-hash filter. | Flags conflicting bibliographic values among grouped catalog rows. |
| `is_required_inferred_conflict` | `boolean` | TRUE or FALSE | `master_material.is_required_inferred_conflict` retained after the deterministic section-hash filter. | Whether grouped rows disagree on inferred-required status. |
| `is_oer_conflict` | `boolean` | TRUE or FALSE | `master_material.is_oer_conflict` retained after the deterministic section-hash filter. | Whether grouped rows disagree on open-resource classification. |
| `is_ia_conflict` | `boolean` | TRUE or FALSE | `master_material.is_ia_conflict` retained after the deterministic section-hash filter. | Whether grouped rows disagree on inclusive-access classification. |
| `bookstore_url` | `varchar` | Absolute `http://` or `https://` bookstore URL | `master_material.bookstore_url` retained after the deterministic section-hash filter. | Bookstore page providing the observed material prices. |
| `price_buy_new_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_new_physical` retained after the deterministic section-hash filter. | Price for new physical purchase offerings. |
| `price_buy_new_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_new_digital` retained after the deterministic section-hash filter. | Price for new digital purchase offerings. |
| `price_buy_new_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_new_na` retained after the deterministic section-hash filter. | Price for new unspecified-format purchase offerings. |
| `price_buy_used_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_used_physical` retained after the deterministic section-hash filter. | Price for used physical purchase offerings. |
| `price_buy_used_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_used_digital` retained after the deterministic section-hash filter. | Price for used digital purchase offerings. |
| `price_buy_used_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_used_na` retained after the deterministic section-hash filter. | Price for used unspecified-format purchase offerings. |
| `price_buy_na_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_na_physical` retained after the deterministic section-hash filter. | Price for unspecified-condition physical purchase offerings. |
| `price_buy_na_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_na_digital` retained after the deterministic section-hash filter. | Price for unspecified-condition digital purchase offerings. |
| `price_buy_na_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_na_na` retained after the deterministic section-hash filter. | Price for unspecified-condition unspecified-format purchase offerings. |
| `price_rental_new_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_new_physical` retained after the deterministic section-hash filter. | Price for new physical rental offerings. |
| `price_rental_new_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_new_digital` retained after the deterministic section-hash filter. | Price for new digital rental offerings. |
| `price_rental_new_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_new_na` retained after the deterministic section-hash filter. | Price for new unspecified-format rental offerings. |
| `price_rental_used_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_used_physical` retained after the deterministic section-hash filter. | Price for used physical rental offerings. |
| `price_rental_used_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_used_digital` retained after the deterministic section-hash filter. | Price for used digital rental offerings. |
| `price_rental_used_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_used_na` retained after the deterministic section-hash filter. | Price for used unspecified-format rental offerings. |
| `price_rental_na_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_na_physical` retained after the deterministic section-hash filter. | Price for unspecified-condition physical rental offerings. |
| `price_rental_na_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_na_digital` retained after the deterministic section-hash filter. | Price for unspecified-condition digital rental offerings. |
| `price_rental_na_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_rental_na_na` retained after the deterministic section-hash filter. | Price for unspecified-condition unspecified-format rental offerings. |
| `format_count` | `bigint` | COUNT(DISTINCT option × condition × format) FILTERed to buy/rental; counts offered tuples independently of price validity, so it can exceed the number of non-NULL pivot cells; max 18 | `master_material.format_count` retained after the deterministic section-hash filter. | Distinct option-condition-format combinations offered for the material. |
| `has_buy` | `boolean` | BOOL_OR(book_option = buy) — a buy listing is offered (option presence, independent of price validity) | `master_material.has_buy` retained after the deterministic section-hash filter. | Whether any purchase offer exists for the material. |
| `has_rent` | `boolean` | BOOL_OR(book_option = rental) — a rental listing is offered (≫ rental_days_min coverage, since many rentals lack a term) | `master_material.has_rent` retained after the deterministic section-hash filter. | Whether any rental offer exists for the material. |
| `price_min` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_min` retained after the deterministic section-hash filter. | Lowest valid price across available material offerings. |
| `price_max` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_max` retained after the deterministic section-hash filter. | Highest valid price across available material offerings. |
| `price_avg` | `double` | USD midpoint `(price_min + price_max) / 2.0`, not `AVG()` | `master_material.price_avg` retained after the deterministic section-hash filter. | Legacy midpoint between minimum and maximum material prices. |
| `rental_days_min` | `integer` | shortest rental term offered for this (section, isbn) | `master_material.rental_days_min` retained after the deterministic section-hash filter. | Shortest available rental term measured in days. |
| `rental_days_max` | `integer` | longest rental term offered (~95 distinct values, summarized as range) | `master_material.rental_days_max` retained after the deterministic section-hash filter. | Longest available rental term measured in days. |
| `price_buy_min` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_min` retained after the deterministic section-hash filter. | Lowest valid purchase price across available material offerings. |
| `price_buy_max` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `master_material.price_buy_max` retained after the deterministic section-hash filter. | Highest valid purchase price across available material offerings. |
| `has_pricing_match` | `boolean` | TRUE when the LEFT pricing_wide join found a section/ISBN row; independent of whether that row has a valid price_min | `master_material.has_pricing_match` retained after the deterministic section-hash filter. | Whether an exact section-and-ISBN pricing match exists. |
| `is_section_required_direct` | `boolean` | TRUE/FALSE for 2024+ section context; NULL before 2024 | `master_material.is_section_required_direct` retained after the deterministic section-hash filter. | Whether the source section has direct required evidence. |
| `panel_source_row_count` | `bigint` | Non-negative whole-number count | `master_material.panel_source_row_count` retained after the deterministic section-hash filter. | Panel-history rows collapsed into the contact lookup. |
| `panel_response_year_variant_count` | `bigint` | Non-negative whole-number count | `master_material.panel_response_year_variant_count` retained after the deterministic section-hash filter. | Distinct campaign labels found among grouped panel-history rows. |
| `use_source_row_count` | `bigint` | Non-negative whole-number count | `master_material.use_source_row_count` retained after the deterministic section-hash filter. | Included catalog rows collapsed into the canonical item. |
| `no_use_source_row_count` | `bigint` | Non-negative whole-number count | `master_material.no_use_source_row_count` retained after the deterministic section-hash filter. | Excluded catalog rows collapsed into the canonical item. |
| `has_use_source_row` | `boolean` | TRUE or FALSE | `master_material.has_use_source_row` retained after the deterministic section-hash filter. | Whether grouped rows include retained-population evidence. |
| `has_no_use_source_row` | `boolean` | TRUE or FALSE | `master_material.has_no_use_source_row` retained after the deterministic section-hash filter. | Whether grouped rows include excluded-population evidence. |
| `population_classification_conflict` | `boolean` | TRUE or FALSE | `master_material.population_classification_conflict` retained after the deterministic section-hash filter. | Flags simultaneous Use and NoUse evidence within groups. |
| `instructor_variant_count` | `bigint` | Non-negative whole-number count | `master_material.instructor_variant_count` retained after the deterministic section-hash filter. | Distinct instructor variants found within grouped catalog rows. |
| `email_variant_count` | `bigint` | Non-negative whole-number count | `master_material.email_variant_count` retained after the deterministic section-hash filter. | Distinct email variants found within grouped catalog rows. |
| `contact_metadata_conflict` | `boolean` | TRUE or FALSE | `master_material.contact_metadata_conflict` retained after the deterministic section-hash filter. | Flags conflicting instructor or email values within groups. |
| `is_supply_conflict` | `boolean` | TRUE or FALSE | `master_material.is_supply_conflict` retained after the deterministic section-hash filter. | Whether grouped rows disagree on supply classification. |
| `no_details_conflict` | `boolean` | TRUE or FALSE | `master_material.no_details_conflict` retained after the deterministic section-hash filter. | Flags disagreement about the no-details placeholder within groups. |
| `no_materials_conflict` | `boolean` | TRUE or FALSE | `master_material.no_materials_conflict` retained after the deterministic section-hash filter. | Flags disagreement about no-materials evidence within groups. |
| `is_canada_conflict` | `boolean` | TRUE or FALSE | `master_material.is_canada_conflict` retained after the deterministic section-hash filter. | Whether grouped rows disagree on Canadian classification. |
| `is_null_isbn_audit` | `boolean` | TRUE or FALSE | `master_material.is_null_isbn_audit` retained after the deterministic section-hash filter. | Whether the row audits a NULL-ISBN source group. |
| `has_nonnull_isbn_in_section` | `boolean` | TRUE or FALSE | `master_material.has_nonnull_isbn_in_section` retained after the deterministic section-hash filter. | Whether the section contains another non-NULL ISBN. |
| `is_no_adoption_section` | `boolean` | TRUE or FALSE | `master_material.is_no_adoption_section` retained after the deterministic section-hash filter. | Whether the section has no non-NULL adopted ISBN. |
