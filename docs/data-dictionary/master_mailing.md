---
notion-id: 3ced9fdd-1a1a-81c8-8775-cfdd56f1d5b0
notion-url: https://app.notion.com/p/master_mailing-3ced9fdd1a1a81c88775cfdd56f1d5b0
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `master_mailing` data dictionary

- Relation kind: table
- Grain / key: One non-NULL, nonblank cleaned email
- Pipeline stage: EDA mailing / 3_mailing_lists.sql
- Direct upstream relations: `course_catalog_20251215`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `unit_id` | `bigint` | IPEDS institution identifier | Deterministically selected `course_catalog_20251215.unit_id` directly from `${SURVEY_TABLE}` for each cleaned email. | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution-name text; source spelling and casing retained | Deterministically selected `course_catalog_20251215.school` directly from `${SURVEY_TABLE}` for each cleaned email. | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | Deterministically selected `course_catalog_20251215.state` directly from `${SURVEY_TABLE}` for each cleaned email. | State or province code for the institution. |
| `department` | `varchar` | Academic department label, such as `Biology` | Deterministically selected `course_catalog_20251215.department` directly from `${SURVEY_TABLE}` for each cleaned email. | Academic department responsible for the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | Deterministically selected `course_catalog_20251215.course_level` directly from `${SURVEY_TABLE}` for each cleaned email. | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject label, such as `Biology` | Deterministically selected `course_catalog_20251215.course_subject` directly from `${SURVEY_TABLE}` for each cleaned email. | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | Deterministically selected `course_catalog_20251215.period` directly from `${SURVEY_TABLE}` for each cleaned email. | Human-readable academic term label from the source. |
| `period_sortable` | `varchar` | `YYYY-N`; 1=Winter, 2=Spring, 3=Summer, 4=Fall | Deterministically selected `course_catalog_20251215.period_sortable` directly from `${SURVEY_TABLE}` for each cleaned email. | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | Deterministically selected `course_catalog_20251215.period_date` directly from `${SURVEY_TABLE}` for each cleaned email. | Canonical starting date assigned to the academic term. |
| `instructor` | `varchar` | Instructor-name text; source punctuation and casing retained | Deterministically selected `course_catalog_20251215.instructor` directly from `${SURVEY_TABLE}` for each cleaned email. | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given-name text; source punctuation and casing retained | Deterministically selected `course_catalog_20251215.first_name` directly from `${SURVEY_TABLE}` for each cleaned email. | Given name of the course instructor. |
| `last_name` | `varchar` | Family-name text; source punctuation and casing retained | Deterministically selected `course_catalog_20251215.last_name` directly from `${SURVEY_TABLE}` for each cleaned email. | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | Deterministically selected `course_catalog_20251215.email` directly from `${SURVEY_TABLE}` for each cleaned email. | Normalized instructor email used for contact and matching. |
