---
notion-id: 3ced9fdd-1a1a-81b0-9fd1-e263cd84db5a
notion-url: https://app.notion.com/p/current_mailing_ny-3ced9fdd1a1a81b09fd1e263cd84db5a
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `current_mailing_ny` data dictionary

- Relation kind: view
- Grain / key: Filtered current_mailing rows
- Pipeline stage: EDA mailing / 3_mailing_lists.sql
- Direct upstream relations: `current_mailing`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `unit_id` | `bigint` | IPEDS institution identifier | Inherited unchanged from `current_mailing.unit_id` (`SELECT *`). | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution-name text; source spelling and casing retained | Inherited unchanged from `current_mailing.school` (`SELECT *`). | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | Inherited unchanged from `current_mailing.state` (`SELECT *`). | State or province code for the institution. |
| `department` | `varchar` | Academic department label, such as `Biology` | Inherited unchanged from `current_mailing.department` (`SELECT *`). | Academic department responsible for the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | Inherited unchanged from `current_mailing.course_level` (`SELECT *`). | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject label, such as `Biology` | Inherited unchanged from `current_mailing.course_subject` (`SELECT *`). | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | Inherited unchanged from `current_mailing.period` (`SELECT *`). | Human-readable academic term label from the source. |
| `period_sortable` | `varchar` | `YYYY-N`; 1=Winter, 2=Spring, 3=Summer, 4=Fall | Inherited unchanged from `current_mailing.period_sortable` (`SELECT *`). | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | Inherited unchanged from `current_mailing.period_date` (`SELECT *`). | Canonical starting date assigned to the academic term. |
| `instructor` | `varchar` | Instructor-name text; source punctuation and casing retained | Inherited unchanged from `current_mailing.instructor` (`SELECT *`). | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given-name text; source punctuation and casing retained | Inherited unchanged from `current_mailing.first_name` (`SELECT *`). | Given name of the course instructor. |
| `last_name` | `varchar` | Family-name text; source punctuation and casing retained | Inherited unchanged from `current_mailing.last_name` (`SELECT *`). | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | Inherited unchanged from `current_mailing.email` (`SELECT *`). | Normalized instructor email used for contact and matching. |
| `panel_response_year` | `varchar` | Campaign label shaped `OER_YYYY`, such as `OER_2025` | Inherited unchanged from `current_mailing.panel_response_year` (`SELECT *`). | Latest recorded panel response campaign label. |
