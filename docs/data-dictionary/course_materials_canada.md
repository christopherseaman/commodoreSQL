---
notion-id: 3ced9fdd-1a1a-8138-be6f-e07ac29571f5
notion-url: https://app.notion.com/p/course_materials_canada-3ced9fdd1a1a8138be6fe07ac29571f5
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `course_materials_canada` data dictionary

- Relation kind: view
- Grain / key: Filtered course_materials rows
- Pipeline stage: Canonical materials / 2b_course_materials.sql
- Direct upstream relations: `course_materials`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `isbn13` | `bigint` | 13-digit numeric ISBN or numeric source identifier; NULL when absent | Inherited unchanged from `course_materials.isbn13` (`SELECT *`). | Book or material identifier used for cross-source matching. |
| `book_title` | `varchar` | Material title text; source punctuation and casing retained | Inherited unchanged from `course_materials.book_title` (`SELECT *`). | Canonical title selected for the grouped ISBN. |
| `author` | `varchar` | Person or organization name; source punctuation and casing retained | Inherited unchanged from `course_materials.author` (`SELECT *`). | Author credited for the adopted course material. |
| `publisher` | `varchar` | Publisher-name text; original spelling and casing retained | Inherited unchanged from `course_materials.publisher` (`SELECT *`). | Publisher credited for the adopted course material. |
| `imprint` | `varchar` | Publishing-imprint text; original spelling and casing retained | Inherited unchanged from `course_materials.imprint` (`SELECT *`). | Publishing imprint named for the adopted material. |
| `book_format` | `varchar` | Source material format; pricing uses `physical`, `digital`, or unavailable format | Inherited unchanged from `course_materials.book_format` (`SELECT *`). | Physical or digital format of the material. |
| `format_type` | `varchar` | Catalog format classification label; original spelling retained | Inherited unchanged from `course_materials.format_type` (`SELECT *`). | Catalog classification label for the material format. |
| `book_status` | `varchar` | Lowercase `required`, `recommended`, `option`, or missing status | Inherited unchanged from `course_materials.book_status` (`SELECT *`). | Adoption priority assigned to the course material. |
| `isbn_book_title` | `varchar` | Canonical title text selected for the term and ISBN | Inherited unchanged from `course_materials.isbn_book_title` (`SELECT *`). | Canonical title selected for the term-level ISBN. |
| `isbn_author` | `varchar` | Canonical author text selected for the term and ISBN | Inherited unchanged from `course_materials.isbn_author` (`SELECT *`). | Canonical author selected for the term-level ISBN. |
| `isbn_publisher` | `varchar` | Canonical publisher text selected for the term and ISBN | Inherited unchanged from `course_materials.isbn_publisher` (`SELECT *`). | Canonical publisher selected for the term-level ISBN. |
| `isbn_title_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.isbn_title_variant_count` (`SELECT *`). | Distinct ISBN title variants found within grouped catalog rows. |
| `isbn_author_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.isbn_author_variant_count` (`SELECT *`). | Distinct ISBN author variants found within grouped catalog rows. |
| `isbn_publisher_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.isbn_publisher_variant_count` (`SELECT *`). | Distinct ISBN publisher variants found within grouped catalog rows. |
| `unit_id` | `bigint` | IPEDS institution identifier | Inherited unchanged from `course_materials.unit_id` (`SELECT *`). | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution-name text; source spelling and casing retained | Inherited unchanged from `course_materials.school` (`SELECT *`). | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | Inherited unchanged from `course_materials.state` (`SELECT *`). | State or province code for the institution. |
| `dept_code` | `varchar` | Short department code, such as `BIOL` | Inherited unchanged from `course_materials.dept_code` (`SELECT *`). | Short code identifying the academic department. |
| `department` | `varchar` | Academic department label, such as `Biology` | Inherited unchanged from `course_materials.department` (`SELECT *`). | Academic department responsible for the course. |
| `dept_description` | `varchar` | Expanded source description of the academic department | Inherited unchanged from `course_materials.dept_description` (`SELECT *`). | Expanded description of the academic department. |
| `course_number` | `varchar` | Source course number; leading zeros and suffixes retained | Inherited unchanged from `course_materials.course_number` (`SELECT *`). | Catalog number identifying the course within its department. |
| `section` | `varchar` | Source section code; leading zeros and punctuation retained | Inherited unchanged from `course_materials.section` (`SELECT *`). | Source code distinguishing sections of the same course. |
| `course_title` | `varchar` | Course title text; source punctuation and casing retained | Inherited unchanged from `course_materials.course_title` (`SELECT *`). | Official title assigned to the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | Inherited unchanged from `course_materials.course_level` (`SELECT *`). | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject label, such as `Biology` | Inherited unchanged from `course_materials.course_subject` (`SELECT *`). | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | Inherited unchanged from `course_materials.period` (`SELECT *`). | Human-readable academic term label from the source. |
| `enrollments` | `integer` | Reported student count; source noise can include negative values | Inherited unchanged from `course_materials.enrollments` (`SELECT *`). | Enrollment reported directly for the course section. |
| `seats_taken` | `integer` | Reported occupied seats; `9999` is the source sentinel | Inherited unchanged from `course_materials.seats_taken` (`SELECT *`). | Occupied seats reported for the course section. |
| `instructor` | `varchar` | Instructor-name text; source punctuation and casing retained | Inherited unchanged from `course_materials.instructor` (`SELECT *`). | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given-name text; source punctuation and casing retained | Inherited unchanged from `course_materials.first_name` (`SELECT *`). | Given name of the course instructor. |
| `last_name` | `varchar` | Family-name text; source punctuation and casing retained | Inherited unchanged from `course_materials.last_name` (`SELECT *`). | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | Inherited unchanged from `course_materials.email` (`SELECT *`). | Normalized instructor email used for contact and matching. |
| `course_id` | `varchar` | `unit_id::department-code::course-number` composite | Inherited unchanged from `course_materials.course_id` (`SELECT *`). | Stable identifier for the institution-level course offering. |
| `section_id` | `varchar` | period-specific section key | Inherited unchanged from `course_materials.section_id` (`SELECT *`). | Period-specific identifier for the distinct section offering. |
| `period_sortable` | `varchar` | YYYY-N | Inherited unchanged from `course_materials.period_sortable` (`SELECT *`). | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | Inherited unchanged from `course_materials.period_date` (`SELECT *`). | Canonical starting date assigned to the academic term. |
| `is_oer` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_oer` (`SELECT *`). | Whether the material is an open educational resource. |
| `oer_category` | `varchar` | Category such as `book_oer`, `pure_oer`, or `non_oer` | Inherited unchanged from `course_materials.oer_category` (`SELECT *`). | Open-resource category assigned to the material format. |
| `is_ia` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_ia` (`SELECT *`). | Whether the material uses inclusive access. |
| `ia_category` | `varchar` | Category such as `book_ia`, `ebook_ia`, or `non_ia` | Inherited unchanged from `course_materials.ia_category` (`SELECT *`). | Inclusive-access category assigned to the material format. |
| `is_supply` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_supply` (`SELECT *`). | Whether the material is classified as a course supply. |
| `supply_category` | `varchar` | Supply category label selected by keyword rules | Inherited unchanged from `course_materials.supply_category` (`SELECT *`). | Supply classification assigned from matched title keywords. |
| `institution_name` | `varchar` | Official IPEDS institution-name text | Inherited unchanged from `course_materials.institution_name` (`SELECT *`). | Canonical institution name supplied by IPEDS. |
| `sector` | `varchar` | IPEDS sector descriptor, such as `Public, 4-year or above` | Inherited unchanged from `course_materials.sector` (`SELECT *`). | IPEDS sector classification for the institution. |
| `level` | `varchar` | IPEDS label such as `Four or more years` | Inherited unchanged from `course_materials.level` (`SELECT *`). | IPEDS award-level classification for the institution. |
| `control` | `varchar` | IPEDS label such as `Public` or `Private not-for-profit` | Inherited unchanged from `course_materials.control` (`SELECT *`). | Institution ownership and governance classification used for reporting. |
| `size` | `varchar` | IPEDS size-band label, such as `20,000 and above` | Inherited unchanged from `course_materials.size` (`SELECT *`). | IPEDS institutional enrollment-size classification used for reporting. |
| `enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | Inherited unchanged from `course_materials.enrollment_2024` (`SELECT *`). | Total institutional enrollment reported to IPEDS for 2024. |
| `distance_enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | Inherited unchanged from `course_materials.distance_enrollment_2024` (`SELECT *`). | IPEDS 2024 students enrolled in distance education. |
| `institution_type` | `varchar` | Derived institution-type category label | Inherited unchanged from `course_materials.institution_type` (`SELECT *`). | Derived institution type used for reporting groups. |
| `panel_response_year` | `varchar` | Campaign label shaped `OER_YYYY`, such as `OER_2025` | Inherited unchanged from `course_materials.panel_response_year` (`SELECT *`). | Latest recorded panel response campaign label. |
| `panel_source_row_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.panel_source_row_count` (`SELECT *`). | Panel-history rows collapsed into the contact lookup. |
| `panel_response_year_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.panel_response_year_variant_count` (`SELECT *`). | Distinct campaign labels found among grouped panel-history rows. |
| `is_opted_out` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_opted_out` (`SELECT *`). | Whether the contact appears in the opt-out list. |
| `opt_out_source` | `varchar` | BVA source-system label for the opt-out entry | Inherited unchanged from `course_materials.opt_out_source` (`SELECT *`). | BVA source label explaining the opt-out record. |
| `is_required_inferred` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_required_inferred` (`SELECT *`). | Whether the material is treated as required after fallback. |
| `is_post_2024` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_post_2024` (`SELECT *`). | Whether the academic term begins during 2024 or later. |
| `has_isbn` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_isbn` (`SELECT *`). | Whether the material has a non-NULL ISBN. |
| `has_formattype` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_formattype` (`SELECT *`). | Whether the material has a nonblank FormatType classification. |
| `has_enrollment` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_enrollment` (`SELECT *`). | Whether usable enrollment information is available. |
| `has_enrollment_own_seats` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_enrollment_own_seats` (`SELECT *`). | Whether the section reports usable occupied seats. |
| `no_details` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.no_details` (`SELECT *`). | Whether the title carries the no-details placeholder. |
| `no_materials` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.no_materials` (`SELECT *`). | Whether the row explicitly indicates no course materials. |
| `is_canada` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_canada` (`SELECT *`). | Whether the source row belongs to Canada. |
| `is_course_material_use` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_course_material_use` (`SELECT *`). | Whether the material belongs to the analysis population. |
| `is_course_material_no_use` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_course_material_no_use` (`SELECT *`). | Whether the material belongs to the excluded population. |
| `has_enrollment_sibling` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_enrollment_sibling` (`SELECT *`). | Whether a sibling section reports enrollment. |
| `has_enrollment_sibling_seats` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_enrollment_sibling_seats` (`SELECT *`). | Whether a sibling section reports usable occupied seats. |
| `enrollment_assigned` | `integer` | Rounded student count from the ladder; raw negatives can propagate | Inherited unchanged from `course_materials.enrollment_assigned` (`SELECT *`). | Best available section enrollment from the assignment ladder. |
| `enrollment_source` | `varchar` | `own`, `own_seats`, `sibling_enroll`, `sibling_seats`, `class_median`, `level_median`, or `none` | Inherited unchanged from `course_materials.enrollment_source` (`SELECT *`). | Assignment-ladder rung that supplied the section enrollment. |
| `has_book_status_required` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_book_status_required` (`SELECT *`). | Whether grouped rows include explicit required status. |
| `has_book_status_optional_recommended` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_book_status_optional_recommended` (`SELECT *`). | Whether grouped rows include optional or recommended status. |
| `source_row_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.source_row_count` (`SELECT *`). | Catalog rows collapsed into the canonical material item. |
| `title_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.title_variant_count` (`SELECT *`). | Distinct title variants found within grouped catalog rows. |
| `author_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.author_variant_count` (`SELECT *`). | Distinct author variants found within grouped catalog rows. |
| `publisher_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.publisher_variant_count` (`SELECT *`). | Distinct publisher variants found within grouped catalog rows. |
| `imprint_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.imprint_variant_count` (`SELECT *`). | Distinct imprint variants found within grouped catalog rows. |
| `book_format_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.book_format_variant_count` (`SELECT *`). | Distinct book format variants found within grouped catalog rows. |
| `format_type_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.format_type_variant_count` (`SELECT *`). | Distinct FormatType variants found within grouped catalog rows. |
| `book_status_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.book_status_variant_count` (`SELECT *`). | Distinct book status variants found within grouped catalog rows. |
| `catalog_metadata_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.catalog_metadata_conflict` (`SELECT *`). | Flags conflicting bibliographic values among grouped catalog rows. |
| `is_required_inferred_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_required_inferred_conflict` (`SELECT *`). | Whether grouped rows disagree on inferred-required status. |
| `is_oer_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_oer_conflict` (`SELECT *`). | Whether grouped rows disagree on open-resource classification. |
| `is_ia_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_ia_conflict` (`SELECT *`). | Whether grouped rows disagree on inclusive-access classification. |
| `use_source_row_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.use_source_row_count` (`SELECT *`). | Included catalog rows collapsed into the canonical item. |
| `no_use_source_row_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.no_use_source_row_count` (`SELECT *`). | Excluded catalog rows collapsed into the canonical item. |
| `has_use_source_row` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_use_source_row` (`SELECT *`). | Whether grouped rows include retained-population evidence. |
| `has_no_use_source_row` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_no_use_source_row` (`SELECT *`). | Whether grouped rows include excluded-population evidence. |
| `population_classification_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.population_classification_conflict` (`SELECT *`). | Flags simultaneous Use and NoUse evidence within groups. |
| `instructor_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.instructor_variant_count` (`SELECT *`). | Distinct instructor variants found within grouped catalog rows. |
| `email_variant_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `course_materials.email_variant_count` (`SELECT *`). | Distinct email variants found within grouped catalog rows. |
| `contact_metadata_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.contact_metadata_conflict` (`SELECT *`). | Flags conflicting instructor or email values within groups. |
| `is_supply_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_supply_conflict` (`SELECT *`). | Whether grouped rows disagree on supply classification. |
| `no_details_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.no_details_conflict` (`SELECT *`). | Flags disagreement about the no-details placeholder within groups. |
| `no_materials_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.no_materials_conflict` (`SELECT *`). | Flags disagreement about no-materials evidence within groups. |
| `is_canada_conflict` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_canada_conflict` (`SELECT *`). | Whether grouped rows disagree on Canadian classification. |
| `is_null_isbn_audit` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_null_isbn_audit` (`SELECT *`). | Whether the row audits a NULL-ISBN source group. |
| `has_nonnull_isbn_in_section` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.has_nonnull_isbn_in_section` (`SELECT *`). | Whether the section contains another non-NULL ISBN. |
| `is_no_adoption_section` | `boolean` | TRUE or FALSE | Inherited unchanged from `course_materials.is_no_adoption_section` (`SELECT *`). | Whether the section has no non-NULL adopted ISBN. |
