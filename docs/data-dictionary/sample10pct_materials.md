---
notion-id: 3d2d9fdd-1a1a-81f2-9eb7-f380f1ce188f
notion-url: https://app.notion.com/p/sample10pct_materials-3d2d9fdd1a1a81f29eb7f380f1ce188f
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `sample10pct_materials` data dictionary

- Relation kind: table
- Grain / key: One sampled Material Costs period × section × ISBN item
- Pipeline stage: Sampling / models/sample10pct_materials.sql
- Direct upstream relations: `material_costs`, `sample10_section_ids`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `isbn13` | `bigint` | 13-digit numeric ISBN or numeric source identifier; NULL when absent | `material_costs.isbn13` retained for `sample10_section_ids` membership. | Book or material identifier used for cross-source matching. |
| `book_title` | `varchar` | Material title; source punctuation/casing retained | `material_costs.book_title` retained for `sample10_section_ids` membership. | Canonical title selected for the grouped ISBN. |
| `author` | `varchar` | Person/organization name; source punctuation/casing retained | `material_costs.author` retained for `sample10_section_ids` membership. | Author credited for the adopted course material. |
| `publisher` | `varchar` | Publisher name; original spelling/casing retained | `material_costs.publisher` retained for `sample10_section_ids` membership. | Publisher credited for the adopted course material. |
| `imprint` | `varchar` | Publishing imprint; original spelling/casing retained | `material_costs.imprint` retained for `sample10_section_ids` membership. | Publishing imprint named for the adopted material. |
| `book_format` | `varchar` | Source material format; pricing uses `physical`, `digital`, or unavailable format | `material_costs.book_format` retained for `sample10_section_ids` membership. | Physical or digital format of the material. |
| `format_type` | `varchar` | Catalog FormatType label; original spelling retained | `material_costs.format_type` retained for `sample10_section_ids` membership. | Catalog classification label for the material format. |
| `book_status` | `varchar` | Lowercase `required`, `recommended`, `option`, or missing status | `material_costs.book_status` retained for `sample10_section_ids` membership. | Adoption priority assigned to the course material. |
| `isbn_book_title` | `varchar` | Canonical title text selected for the term and ISBN | `material_costs.isbn_book_title` retained for `sample10_section_ids` membership. | Canonical title selected for the term-level ISBN. |
| `isbn_author` | `varchar` | Canonical author text selected for the term and ISBN | `material_costs.isbn_author` retained for `sample10_section_ids` membership. | Canonical author selected for the term-level ISBN. |
| `isbn_publisher` | `varchar` | Canonical publisher text selected for the term and ISBN | `material_costs.isbn_publisher` retained for `sample10_section_ids` membership. | Canonical publisher selected for the term-level ISBN. |
| `isbn_title_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.isbn_title_variant_count` retained for `sample10_section_ids` membership. | Distinct ISBN title variants found within grouped catalog rows. |
| `isbn_author_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.isbn_author_variant_count` retained for `sample10_section_ids` membership. | Distinct ISBN author variants found within grouped catalog rows. |
| `isbn_publisher_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.isbn_publisher_variant_count` retained for `sample10_section_ids` membership. | Distinct ISBN publisher variants found within grouped catalog rows. |
| `unit_id` | `bigint` | IPEDS institution identifier | `material_costs.unit_id` retained for `sample10_section_ids` membership. | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution name; source spelling/casing retained | `material_costs.school` retained for `sample10_section_ids` membership. | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | `material_costs.state` retained for `sample10_section_ids` membership. | State or province code for the institution. |
| `dept_code` | `varchar` | Department code, such as `BIOL` | `material_costs.dept_code` retained for `sample10_section_ids` membership. | Short code identifying the academic department. |
| `department` | `varchar` | Academic department, such as `Biology` | `material_costs.department` retained for `sample10_section_ids` membership. | Academic department responsible for the course. |
| `dept_description` | `varchar` | Expanded source description of the academic department | `material_costs.dept_description` retained for `sample10_section_ids` membership. | Expanded description of the academic department. |
| `course_number` | `varchar` | Source course number; zeros/suffixes retained | `material_costs.course_number` retained for `sample10_section_ids` membership. | Catalog number identifying the course within its department. |
| `section` | `varchar` | Source section code; zeros/punctuation retained | `material_costs.section` retained for `sample10_section_ids` membership. | Source code distinguishing sections of the same course. |
| `course_title` | `varchar` | Course title; source punctuation/casing retained | `material_costs.course_title` retained for `sample10_section_ids` membership. | Official title assigned to the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | `material_costs.course_level` retained for `sample10_section_ids` membership. | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject, such as `Biology` | `material_costs.course_subject` retained for `sample10_section_ids` membership. | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | `material_costs.period` retained for `sample10_section_ids` membership. | Human-readable academic term label from the source. |
| `enrollments` | `integer` | Reported student count; source noise can include negative values | `material_costs.enrollments` retained for `sample10_section_ids` membership. | Enrollment reported directly for the course section. |
| `seats_taken` | `integer` | Reported occupied seats; `9999` is the source sentinel | `material_costs.seats_taken` retained for `sample10_section_ids` membership. | Occupied seats reported for the course section. |
| `instructor` | `varchar` | Instructor name; source punctuation/casing retained | `material_costs.instructor` retained for `sample10_section_ids` membership. | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given name; source punctuation/casing retained | `material_costs.first_name` retained for `sample10_section_ids` membership. | Given name of the course instructor. |
| `last_name` | `varchar` | Family name; source punctuation/casing retained | `material_costs.last_name` retained for `sample10_section_ids` membership. | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | `material_costs.email` retained for `sample10_section_ids` membership. | Normalized instructor email used for contact and matching. |
| `course_id` | `varchar` | `unit_id::department-code::course-number` composite | `material_costs.course_id` retained for `sample10_section_ids` membership. | Stable identifier for the institution-level course offering. |
| `section_id` | `varchar` | period-specific section key | `material_costs.section_id` retained for `sample10_section_ids` membership. | Period-specific identifier for the distinct section offering. |
| `period_sortable` | `varchar` | YYYY-N | `material_costs.period_sortable` retained for `sample10_section_ids` membership. | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | `material_costs.period_date` retained for `sample10_section_ids` membership. | Canonical starting date assigned to the academic term. |
| `is_oer` | `boolean` | TRUE or FALSE | `material_costs.is_oer` retained for `sample10_section_ids` membership. | Whether the material is an open educational resource. |
| `oer_category` | `varchar` | Category such as `book_oer`, `pure_oer`, or `non_oer` | `material_costs.oer_category` retained for `sample10_section_ids` membership. | Open-resource category assigned to the material format. |
| `is_ia` | `boolean` | TRUE or FALSE | `material_costs.is_ia` retained for `sample10_section_ids` membership. | Whether the material uses inclusive access. |
| `ia_category` | `varchar` | Category such as `book_ia`, `ebook_ia`, or `non_ia` | `material_costs.ia_category` retained for `sample10_section_ids` membership. | Inclusive-access category assigned to the material format. |
| `is_supply` | `boolean` | TRUE or FALSE | `material_costs.is_supply` retained for `sample10_section_ids` membership. | Whether the material is classified as a course supply. |
| `supply_category` | `varchar` | Supply category label selected by keyword rules | `material_costs.supply_category` retained for `sample10_section_ids` membership. | Supply classification assigned from matched title keywords. |
| `institution_name` | `varchar` | Official IPEDS institution-name text | `material_costs.institution_name` retained for `sample10_section_ids` membership. | Canonical institution name supplied by IPEDS. |
| `sector` | `varchar` | IPEDS sector descriptor, such as `Public, 4-year or above` | `material_costs.sector` retained for `sample10_section_ids` membership. | IPEDS sector classification for the institution. |
| `level` | `varchar` | IPEDS label such as `Four or more years` | `material_costs.level` retained for `sample10_section_ids` membership. | IPEDS award-level classification for the institution. |
| `control` | `varchar` | IPEDS label such as `Public` or `Private not-for-profit` | `material_costs.control` retained for `sample10_section_ids` membership. | Institution ownership and governance classification used for reporting. |
| `size` | `varchar` | IPEDS size-band label, such as `20,000 and above` | `material_costs.size` retained for `sample10_section_ids` membership. | IPEDS institutional enrollment-size classification used for reporting. |
| `enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | `material_costs.enrollment_2024` retained for `sample10_section_ids` membership. | Total institutional enrollment reported to IPEDS for 2024. |
| `distance_enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | `material_costs.distance_enrollment_2024` retained for `sample10_section_ids` membership. | IPEDS 2024 students enrolled in distance education. |
| `institution_type` | `varchar` | Derived institution-type category label | `material_costs.institution_type` retained for `sample10_section_ids` membership. | Derived institution type used for reporting groups. |
| `panel_response_year` | `varchar` | Campaign label shaped `OER_YYYY`, such as `OER_2025` | `material_costs.panel_response_year` retained for `sample10_section_ids` membership. | Latest recorded panel response campaign label. |
| `is_opted_out` | `boolean` | TRUE or FALSE | `material_costs.is_opted_out` retained for `sample10_section_ids` membership. | Whether the contact appears in the opt-out list. |
| `opt_out_source` | `varchar` | BVA source-system label for the opt-out entry | `material_costs.opt_out_source` retained for `sample10_section_ids` membership. | BVA source label explaining the opt-out record. |
| `is_required_inferred` | `boolean` | TRUE or FALSE | `material_costs.is_required_inferred` retained for `sample10_section_ids` membership. | Whether the material is treated as required after fallback. |
| `is_post_2024` | `boolean` | TRUE or FALSE | `material_costs.is_post_2024` retained for `sample10_section_ids` membership. | Whether the academic term begins during 2024 or later. |
| `has_isbn` | `boolean` | TRUE or FALSE | `material_costs.has_isbn` retained for `sample10_section_ids` membership. | Whether the material has a non-NULL ISBN. |
| `has_formattype` | `boolean` | TRUE or FALSE | `material_costs.has_formattype` retained for `sample10_section_ids` membership. | Whether the material has a nonblank FormatType classification. |
| `has_enrollment` | `boolean` | TRUE or FALSE | `material_costs.has_enrollment` retained for `sample10_section_ids` membership. | Whether usable enrollment information is available. |
| `has_enrollment_own_seats` | `boolean` | TRUE or FALSE | `material_costs.has_enrollment_own_seats` retained for `sample10_section_ids` membership. | Whether the section reports usable occupied seats. |
| `no_details` | `boolean` | TRUE or FALSE | `material_costs.no_details` retained for `sample10_section_ids` membership. | Whether the title carries the no-details placeholder. |
| `no_materials` | `boolean` | TRUE or FALSE | `material_costs.no_materials` retained for `sample10_section_ids` membership. | Whether the row explicitly indicates no course materials. |
| `is_canada` | `boolean` | TRUE or FALSE | `material_costs.is_canada` retained for `sample10_section_ids` membership. | Whether the source row belongs to Canada. |
| `is_course_material_use` | `boolean` | TRUE or FALSE | `material_costs.is_course_material_use` retained for `sample10_section_ids` membership. | Whether the material belongs to the analysis population. |
| `is_course_material_no_use` | `boolean` | TRUE or FALSE | `material_costs.is_course_material_no_use` retained for `sample10_section_ids` membership. | Whether the material belongs to the excluded population. |
| `has_enrollment_sibling` | `boolean` | TRUE or FALSE | `material_costs.has_enrollment_sibling` retained for `sample10_section_ids` membership. | Whether a sibling section reports enrollment. |
| `has_enrollment_sibling_seats` | `boolean` | TRUE or FALSE | `material_costs.has_enrollment_sibling_seats` retained for `sample10_section_ids` membership. | Whether a sibling section reports usable occupied seats. |
| `enrollment_assigned` | `integer` | Rounded student count from the ladder; raw negatives can propagate | `material_costs.enrollment_assigned` retained for `sample10_section_ids` membership. | Best available section enrollment from the assignment ladder. |
| `enrollment_source` | `varchar` | `own`, `own_seats`, `sibling_enroll`, `sibling_seats`, `class_median`, `level_median`, or `none` | `material_costs.enrollment_source` retained for `sample10_section_ids` membership. | Assignment-ladder rung that supplied the section enrollment. |
| `is_required_direct` | `boolean` | TRUE or FALSE | `material_costs.is_required_direct` retained for `sample10_section_ids` membership. | Whether direct required evidence is present at the relation grain. |
| `is_optional_or_recommended_direct` | `boolean` | TRUE or FALSE | `material_costs.is_optional_or_recommended_direct` retained for `sample10_section_ids` membership. | Whether grouped rows include optional or recommended direct-status evidence. |
| `source_row_count` | `bigint` | Non-negative whole-number count | `material_costs.source_row_count` retained for `sample10_section_ids` membership. | Catalog rows collapsed into the canonical material item. |
| `title_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.title_variant_count` retained for `sample10_section_ids` membership. | Distinct title variants found within grouped catalog rows. |
| `author_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.author_variant_count` retained for `sample10_section_ids` membership. | Distinct author variants found within grouped catalog rows. |
| `publisher_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.publisher_variant_count` retained for `sample10_section_ids` membership. | Distinct publisher variants found within grouped catalog rows. |
| `imprint_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.imprint_variant_count` retained for `sample10_section_ids` membership. | Distinct imprint variants found within grouped catalog rows. |
| `book_format_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.book_format_variant_count` retained for `sample10_section_ids` membership. | Distinct book format variants found within grouped catalog rows. |
| `format_type_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.format_type_variant_count` retained for `sample10_section_ids` membership. | Distinct FormatType variants found within grouped catalog rows. |
| `book_status_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.book_status_variant_count` retained for `sample10_section_ids` membership. | Distinct book status variants found within grouped catalog rows. |
| `catalog_metadata_conflict` | `boolean` | TRUE or FALSE | `material_costs.catalog_metadata_conflict` retained for `sample10_section_ids` membership. | Flags conflicting bibliographic values among grouped catalog rows. |
| `is_required_inferred_conflict` | `boolean` | TRUE or FALSE | `material_costs.is_required_inferred_conflict` retained for `sample10_section_ids` membership. | Whether grouped rows disagree on inferred-required status. |
| `is_oer_conflict` | `boolean` | TRUE or FALSE | `material_costs.is_oer_conflict` retained for `sample10_section_ids` membership. | Whether grouped rows disagree on open-resource classification. |
| `is_ia_conflict` | `boolean` | TRUE or FALSE | `material_costs.is_ia_conflict` retained for `sample10_section_ids` membership. | Whether grouped rows disagree on inclusive-access classification. |
| `bookstore_url` | `varchar` | Absolute `http://` or `https://` bookstore URL | `material_costs.bookstore_url` retained for `sample10_section_ids` membership. | Bookstore page providing the observed material prices. |
| `price_buy_new_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_new_physical` retained for `sample10_section_ids` membership. | Price for new physical purchase offerings. |
| `price_buy_new_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_new_digital` retained for `sample10_section_ids` membership. | Price for new digital purchase offerings. |
| `price_buy_new_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_new_na` retained for `sample10_section_ids` membership. | Price for new unspecified-format purchase offerings. |
| `price_buy_used_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_used_physical` retained for `sample10_section_ids` membership. | Price for used physical purchase offerings. |
| `price_buy_used_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_used_digital` retained for `sample10_section_ids` membership. | Price for used digital purchase offerings. |
| `price_buy_used_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_used_na` retained for `sample10_section_ids` membership. | Price for used unspecified-format purchase offerings. |
| `price_buy_na_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_na_physical` retained for `sample10_section_ids` membership. | Price for unspecified-condition physical purchase offerings. |
| `price_buy_na_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_na_digital` retained for `sample10_section_ids` membership. | Price for unspecified-condition digital purchase offerings. |
| `price_buy_na_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_na_na` retained for `sample10_section_ids` membership. | Price for unspecified-condition unspecified-format purchase offerings. |
| `price_rental_new_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_new_physical` retained for `sample10_section_ids` membership. | Price for new physical rental offerings. |
| `price_rental_new_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_new_digital` retained for `sample10_section_ids` membership. | Price for new digital rental offerings. |
| `price_rental_new_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_new_na` retained for `sample10_section_ids` membership. | Price for new unspecified-format rental offerings. |
| `price_rental_used_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_used_physical` retained for `sample10_section_ids` membership. | Price for used physical rental offerings. |
| `price_rental_used_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_used_digital` retained for `sample10_section_ids` membership. | Price for used digital rental offerings. |
| `price_rental_used_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_used_na` retained for `sample10_section_ids` membership. | Price for used unspecified-format rental offerings. |
| `price_rental_na_physical` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_na_physical` retained for `sample10_section_ids` membership. | Price for unspecified-condition physical rental offerings. |
| `price_rental_na_digital` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_na_digital` retained for `sample10_section_ids` membership. | Price for unspecified-condition digital rental offerings. |
| `price_rental_na_na` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_rental_na_na` retained for `sample10_section_ids` membership. | Price for unspecified-condition unspecified-format rental offerings. |
| `format_count` | `bigint` | COUNT(DISTINCT option × condition × format) FILTERed to buy/rental; counts offered tuples independently of price validity, so it can exceed the number of non-NULL pivot cells; max 18 | `material_costs.format_count` retained for `sample10_section_ids` membership. | Distinct option-condition-format combinations offered for the material. |
| `has_buy` | `boolean` | BOOL_OR(book_option = buy) — a buy listing is offered (option presence, independent of price validity) | `material_costs.has_buy` retained for `sample10_section_ids` membership. | Whether any purchase offer exists for the material. |
| `has_rent` | `boolean` | BOOL_OR(book_option = rental) — a rental listing is offered (≫ rental_days_min coverage, since many rentals lack a term) | `material_costs.has_rent` retained for `sample10_section_ids` membership. | Whether any rental offer exists for the material. |
| `price_min` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_min` retained for `sample10_section_ids` membership. | Lowest valid price across available material offerings. |
| `price_max` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_max` retained for `sample10_section_ids` membership. | Highest valid price across available material offerings. |
| `price_avg` | `double` | USD midpoint `(price_min + price_max) / 2.0`, not `AVG()` | `material_costs.price_avg` retained for `sample10_section_ids` membership. | Legacy midpoint between minimum and maximum material prices. |
| `rental_days_min` | `integer` | shortest rental term offered for this (section, isbn) | `material_costs.rental_days_min` retained for `sample10_section_ids` membership. | Shortest available rental term measured in days. |
| `rental_days_max` | `integer` | longest rental term offered (~95 distinct values, summarized as range) | `material_costs.rental_days_max` retained for `sample10_section_ids` membership. | Longest available rental term measured in days. |
| `price_buy_min` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_min` retained for `sample10_section_ids` membership. | Lowest valid purchase price across available material offerings. |
| `price_buy_max` | `decimal(10,2)` | USD amount such as `123.45`; stored as `DECIMAL(10,2)` | `material_costs.price_buy_max` retained for `sample10_section_ids` membership. | Highest valid purchase price across available material offerings. |
| `has_pricing_match` | `boolean` | TRUE when the LEFT pricing_wide join found a section/ISBN row; independent of whether that row has a valid price_min | `material_costs.has_pricing_match` retained for `sample10_section_ids` membership. | Whether an exact section-and-ISBN pricing match exists. |
| `is_section_required_direct` | `boolean` | TRUE/FALSE for 2024+ section context; NULL before 2024 | `material_costs.is_section_required_direct` retained for `sample10_section_ids` membership. | Whether the source section has direct required evidence. |
| `panel_source_row_count` | `bigint` | Non-negative whole-number count | `material_costs.panel_source_row_count` retained for `sample10_section_ids` membership. | Panel-history rows collapsed into the contact lookup. |
| `panel_response_year_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.panel_response_year_variant_count` retained for `sample10_section_ids` membership. | Distinct campaign labels found among grouped panel-history rows. |
| `use_source_row_count` | `bigint` | Non-negative whole-number count | `material_costs.use_source_row_count` retained for `sample10_section_ids` membership. | Included catalog rows collapsed into the canonical item. |
| `no_use_source_row_count` | `bigint` | Non-negative whole-number count | `material_costs.no_use_source_row_count` retained for `sample10_section_ids` membership. | Excluded catalog rows collapsed into the canonical item. |
| `has_use_source_row` | `boolean` | TRUE or FALSE | `material_costs.has_use_source_row` retained for `sample10_section_ids` membership. | Whether grouped rows include retained-population evidence. |
| `has_no_use_source_row` | `boolean` | TRUE or FALSE | `material_costs.has_no_use_source_row` retained for `sample10_section_ids` membership. | Whether grouped rows include excluded-population evidence. |
| `population_classification_conflict` | `boolean` | TRUE or FALSE | `material_costs.population_classification_conflict` retained for `sample10_section_ids` membership. | Flags simultaneous Use and NoUse evidence within groups. |
| `instructor_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.instructor_variant_count` retained for `sample10_section_ids` membership. | Distinct instructor variants found within grouped catalog rows. |
| `email_variant_count` | `bigint` | Non-negative whole-number count | `material_costs.email_variant_count` retained for `sample10_section_ids` membership. | Distinct email variants found within grouped catalog rows. |
| `contact_metadata_conflict` | `boolean` | TRUE or FALSE | `material_costs.contact_metadata_conflict` retained for `sample10_section_ids` membership. | Flags conflicting instructor or email values within groups. |
| `is_supply_conflict` | `boolean` | TRUE or FALSE | `material_costs.is_supply_conflict` retained for `sample10_section_ids` membership. | Whether grouped rows disagree on supply classification. |
| `no_details_conflict` | `boolean` | TRUE or FALSE | `material_costs.no_details_conflict` retained for `sample10_section_ids` membership. | Flags disagreement about the no-details placeholder within groups. |
| `no_materials_conflict` | `boolean` | TRUE or FALSE | `material_costs.no_materials_conflict` retained for `sample10_section_ids` membership. | Flags disagreement about no-materials evidence within groups. |
| `is_canada_conflict` | `boolean` | TRUE or FALSE | `material_costs.is_canada_conflict` retained for `sample10_section_ids` membership. | Whether grouped rows disagree on Canadian classification. |
| `is_null_isbn_audit` | `boolean` | TRUE or FALSE | `material_costs.is_null_isbn_audit` retained for `sample10_section_ids` membership. | Whether the row audits a NULL-ISBN source group. |
| `has_nonnull_isbn_in_section` | `boolean` | TRUE or FALSE | `material_costs.has_nonnull_isbn_in_section` retained for `sample10_section_ids` membership. | Whether the section contains another non-NULL ISBN. |
| `is_no_adoption_section` | `boolean` | TRUE or FALSE | `material_costs.is_no_adoption_section` retained for `sample10_section_ids` membership. | Whether the section has no non-NULL adopted ISBN. |
