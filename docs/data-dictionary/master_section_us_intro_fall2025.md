---
notion-id: 3ced9fdd-1a1a-81f8-aa97-fe95d361af27
notion-url: https://app.notion.com/p/master_section_us_intro_fall2025-3ced9fdd1a1a81f8aa97fe95d361af27
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `master_section_us_intro_fall2025` data dictionary

- Relation kind: view
- Grain / key: Filtered master_section rows
- Pipeline stage: EDA records / 4_merged_records.sql
- Direct upstream relations: `master_section`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `section_id` | `varchar` | `course_id::section-code::period_sortable` composite | Inherited unchanged from `master_section.section_id` (`SELECT *`). | Period-specific identifier for the distinct section offering. |
| `course_id` | `varchar` | `unit_id::department-code::course-number` composite | Inherited unchanged from `master_section.course_id` (`SELECT *`). | Stable identifier for the institution-level course offering. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | Inherited unchanged from `master_section.period` (`SELECT *`). | Human-readable academic term label from the source. |
| `period_sortable` | `varchar` | `YYYY-N`; 1=Winter, 2=Spring, 3=Summer, 4=Fall | Inherited unchanged from `master_section.period_sortable` (`SELECT *`). | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | Inherited unchanged from `master_section.period_date` (`SELECT *`). | Canonical starting date assigned to the academic term. |
| `unit_id` | `bigint` | IPEDS institution identifier | Inherited unchanged from `master_section.unit_id` (`SELECT *`). | IPEDS institution identifier used throughout the pipeline. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | Inherited unchanged from `master_section.state` (`SELECT *`). | State or province code for the institution. |
| `control` | `varchar` | IPEDS label such as `Public` or `Private not-for-profit` | Inherited unchanged from `master_section.control` (`SELECT *`). | Institution ownership and governance classification used for reporting. |
| `level` | `varchar` | IPEDS label such as `Four or more years` | Inherited unchanged from `master_section.level` (`SELECT *`). | IPEDS award-level classification for the institution. |
| `size` | `varchar` | IPEDS size-band label, such as `20,000 and above` | Inherited unchanged from `master_section.size` (`SELECT *`). | IPEDS institutional enrollment-size classification used for reporting. |
| `sector` | `varchar` | IPEDS sector descriptor, such as `Public, 4-year or above` | Inherited unchanged from `master_section.sector` (`SELECT *`). | IPEDS sector classification for the institution. |
| `institution_name` | `varchar` | Official IPEDS institution-name text | Inherited unchanged from `master_section.institution_name` (`SELECT *`). | Canonical institution name supplied by IPEDS. |
| `institution_type` | `varchar` | Derived institution-type category label | Inherited unchanged from `master_section.institution_type` (`SELECT *`). | Derived institution type used for reporting groups. |
| `enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | Inherited unchanged from `master_section.enrollment_2024` (`SELECT *`). | Total institutional enrollment reported to IPEDS for 2024. |
| `distance_enrollment_2024` | `integer` | Non-negative 2024 student count; NULL when unavailable | Inherited unchanged from `master_section.distance_enrollment_2024` (`SELECT *`). | IPEDS 2024 students enrolled in distance education. |
| `school` | `varchar` | Institution-name text; source spelling and casing retained | Inherited unchanged from `master_section.school` (`SELECT *`). | Institution or school name attached to the course. |
| `department` | `varchar` | Academic department label, such as `Biology` | Inherited unchanged from `master_section.department` (`SELECT *`). | Academic department responsible for the course. |
| `course_number` | `varchar` | Source course number; leading zeros and suffixes retained | Inherited unchanged from `master_section.course_number` (`SELECT *`). | Catalog number identifying the course within its department. |
| `section` | `varchar` | Source section code; leading zeros and punctuation retained | Inherited unchanged from `master_section.section` (`SELECT *`). | Source code distinguishing sections of the same course. |
| `course_title` | `varchar` | Course title text; source punctuation and casing retained | Inherited unchanged from `master_section.course_title` (`SELECT *`). | Official title assigned to the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | Inherited unchanged from `master_section.course_level` (`SELECT *`). | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject label, such as `Biology` | Inherited unchanged from `master_section.course_subject` (`SELECT *`). | Subject area assigned to the course. |
| `material_count` | `bigint` | COUNT of canonical material_costs section×ISBN items; always >0 | Inherited unchanged from `master_section.material_count` (`SELECT *`). | Canonical course-material items within the section. |
| `required_count` | `bigint` | COUNT of material_costs items WHERE is_required_inferred; A/B split key | Inherited unchanged from `master_section.required_count` (`SELECT *`). | Required materials within the canonical section. |
| `optional_count` | `bigint` | COUNT of material_costs items WHERE NOT is_required_inferred | Inherited unchanged from `master_section.optional_count` (`SELECT *`). | Optional or recommended materials within the section. |
| `has_course_material_use` | `boolean` | Always true: membership requires at least one canonical Use item | Inherited unchanged from `master_section.has_course_material_use` (`SELECT *`). | Whether the section contains retained canonical materials. |
| `course_material_use_count` | `bigint` | Canonical material items; equals material_count | Inherited unchanged from `master_section.course_material_use_count` (`SELECT *`). | Canonical included materials within the retained section. |
| `course_material_no_use_count` | `bigint` | Co-occurring NoUse catalog rows for retained sections only; sidecar audit | Inherited unchanged from `master_section.course_material_no_use_count` (`SELECT *`). | Excluded material rows co-occurring with retained sections. |
| `no_details_count` | `bigint` | Co-occurring no_details rows for retained sections only; sidecar audit | Inherited unchanged from `master_section.no_details_count` (`SELECT *`). | No-details placeholder rows co-occurring with retained sections. |
| `no_materials_count` | `bigint` | Co-occurring no_materials rows for retained sections only; sidecar audit | Inherited unchanged from `master_section.no_materials_count` (`SELECT *`). | No-materials placeholder rows co-occurring with retained sections. |
| `is_canada` | `boolean` | Co-occurring Canada row for a retained section; sidecar audit, not a full-population indicator | Inherited unchanged from `master_section.is_canada` (`SELECT *`). | Whether the source row belongs to Canada. |
| `is_supply` | `boolean` | #36: co-occurring classified supply on a retained section; sidecar audit | Inherited unchanged from `master_section.is_supply` (`SELECT *`). | Whether the material is classified as a course supply. |
| `supply_count` | `bigint` | #36: co-occurring classified supply rows on a retained section; sidecar audit | Inherited unchanged from `master_section.supply_count` (`SELECT *`). | Classified supply rows co-occurring with retained sections. |
| `is_oer` | `boolean` | COALESCE(BOOL_OR(is_oer), FALSE) over canonical material_costs items | Inherited unchanged from `master_section.is_oer` (`SELECT *`). | Whether the material is an open educational resource. |
| `is_ia` | `boolean` | TRUE or FALSE | Inherited unchanged from `master_section.is_ia` (`SELECT *`). | Whether the material uses inclusive access. |
| `oer_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `master_section.oer_count` (`SELECT *`). | Open-resource materials within the aggregation group. |
| `ia_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `master_section.ia_count` (`SELECT *`). | Inclusive-access materials within the aggregation group. |
| `publishers` | `varchar[]` | LIST(DISTINCT publisher) | Inherited unchanged from `master_section.publishers` (`SELECT *`). | Distinct publishers represented by the section materials. |
| `required_publishers` | `varchar[]` | LIST(DISTINCT publisher) WHERE required | Inherited unchanged from `master_section.required_publishers` (`SELECT *`). | Distinct publishers of required materials within the section. |
| `required_publisher_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `master_section.required_publisher_count` (`SELECT *`). | Distinct publishers of required materials within the section. |
| `optional_publisher_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `master_section.optional_publisher_count` (`SELECT *`). | Distinct publishers of optional materials within the section. |
| `enrollments` | `integer` | Reported student count; source noise can include negative values | Inherited unchanged from `master_section.enrollments` (`SELECT *`). | Enrollment reported directly for the course section. |
| `seats_taken` | `integer` | Reported occupied seats; `9999` is the source sentinel | Inherited unchanged from `master_section.seats_taken` (`SELECT *`). | Occupied seats reported for the course section. |
| `has_isbn` | `boolean` | BOOL_OR(has_isbn) over canonical items; true under the current Use contract | Inherited unchanged from `master_section.has_isbn` (`SELECT *`). | Whether the material has a non-NULL ISBN. |
| `has_formattype` | `boolean` | BOOL_OR(FormatType present) over canonical items; OER/IA classifiability flag | Inherited unchanged from `master_section.has_formattype` (`SELECT *`). | Whether the material has a nonblank FormatType classification. |
| `isbn_count` | `bigint` | canonical ISBN items; currently equals material_count | Inherited unchanged from `master_section.isbn_count` (`SELECT *`). | ISBN-bearing materials within the aggregation group. |
| `classified_count` | `bigint` | canonical items carrying a FormatType | Inherited unchanged from `master_section.classified_count` (`SELECT *`). | Materials having a nonblank FormatType classification. |
| `has_enrollment` | `boolean` | section carries its own enrollment | Inherited unchanged from `master_section.has_enrollment` (`SELECT *`). | Whether usable enrollment information is available. |
| `has_enrollment_sibling` | `boolean` | another section of the same course+period has enrollment | Inherited unchanged from `master_section.has_enrollment_sibling` (`SELECT *`). | Whether a sibling section reports enrollment. |
| `has_enrollment_own_seats` | `boolean` | this section has usable seats_taken (<9999) | Inherited unchanged from `master_section.has_enrollment_own_seats` (`SELECT *`). | Whether the section reports usable occupied seats. |
| `has_enrollment_sibling_seats` | `boolean` | a sibling section has usable seats_taken | Inherited unchanged from `master_section.has_enrollment_sibling_seats` (`SELECT *`). | Whether a sibling section reports usable occupied seats. |
| `enrollment_assigned` | `integer` | Rounded student count from the ladder; raw negatives can propagate | Inherited unchanged from `master_section.enrollment_assigned` (`SELECT *`). | Best available section enrollment from the assignment ladder. |
| `enrollment_source` | `varchar` | #32: which fill rung supplied enrollment_assigned: own \| own_seats \| sibling_enroll \| sibling_seats \| class_median \| level_median \| none | Inherited unchanged from `master_section.enrollment_source` (`SELECT *`). | Assignment-ladder rung that supplied the section enrollment. |
| `required_cost_total_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.required_cost_total_min` (`SELECT *`). | Lower total cost bound for required materials. |
| `required_cost_total_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.required_cost_total_max` (`SELECT *`). | Upper total cost bound for required materials. |
| `optional_cost_total_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.optional_cost_total_min` (`SELECT *`). | Lower total cost bound for optional materials. |
| `optional_cost_total_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.optional_cost_total_max` (`SELECT *`). | Upper total cost bound for optional materials. |
| `required_cost_owned_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.required_cost_owned_min` (`SELECT *`). | Lower buy-only cost bound for required materials. |
| `required_cost_owned_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.required_cost_owned_max` (`SELECT *`). | Upper buy-only cost bound for required materials. |
| `optional_cost_owned_min` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.optional_cost_owned_min` (`SELECT *`). | Lower buy-only cost bound for optional materials. |
| `optional_cost_owned_max` | `decimal(38,2)` | USD amount such as `123.45`; stored as `DECIMAL(38,2)` | Inherited unchanged from `master_section.optional_cost_owned_max` (`SELECT *`). | Upper buy-only cost bound for optional materials. |
| `required_cost_avg` | `double` | USD midpoint of lower and upper total cost bounds | Inherited unchanged from `master_section.required_cost_avg` (`SELECT *`). | Legacy midpoint of total required-material cost bounds. |
| `required_cost_owned_avg` | `double` | USD midpoint of lower and upper buy-only cost bounds | Inherited unchanged from `master_section.required_cost_owned_avg` (`SELECT *`). | Legacy midpoint of buy-only required-material cost bounds. |
| `optional_cost_avg` | `double` | USD midpoint of lower and upper total cost bounds | Inherited unchanged from `master_section.optional_cost_avg` (`SELECT *`). | Legacy midpoint of total optional-material cost bounds. |
| `required_priced_count` | `bigint` | distinct required materials with ANY price (total coverage; NOT owned-only — can be >0 while owned cost is NULL when priced materials are rental-only) | Inherited unchanged from `master_section.required_priced_count` (`SELECT *`). | Required materials having at least one valid price. |
| `optional_priced_count` | `bigint` | Non-negative whole-number count | Inherited unchanged from `master_section.optional_priced_count` (`SELECT *`). | Optional materials having at least one valid price. |
