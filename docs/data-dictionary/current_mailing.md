---
notion-id: 3ced9fdd-1a1a-81dd-ba91-d2867a8686fb
notion-url: https://app.notion.com/p/current_mailing-3ced9fdd1a1a81ddba91d2867a8686fb
notion-sync: push
---

<!-- Generated by scripts/generate_data_dictionary.py; do not edit the body. -->

# `current_mailing` data dictionary

- Relation kind: view
- Grain / key: One non-opted-out cleaned email selected in the latest 12 master periods
- Pipeline stage: EDA mailing / 3_mailing_lists.sql
- Direct upstream relations: `master_mailing`, `recent_periods`, `panel_email`, `opt_out`

| Column | Type | Example / structure | Direct upstream source / derivation | Description |
|---|---|---|---|---|
| `unit_id` | `bigint` | IPEDS institution identifier | Passthrough from `master_mailing.unit_id` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | IPEDS institution identifier used throughout the pipeline. |
| `school` | `varchar` | Institution-name text; source spelling and casing retained | Passthrough from `master_mailing.school` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Institution or school name attached to the course. |
| `state` | `varchar` | Normalized state or province code, such as `CA` or `CAN` | Passthrough from `master_mailing.state` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | State or province code for the institution. |
| `department` | `varchar` | Academic department label, such as `Biology` | Passthrough from `master_mailing.department` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Academic department responsible for the course. |
| `course_level` | `varchar` | Category such as `Introductory or general undergraduate` | Passthrough from `master_mailing.course_level` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Instructional level assigned to the course. |
| `course_subject` | `varchar` | Source subject label, such as `Biology` | Passthrough from `master_mailing.course_subject` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Subject area assigned to the course. |
| `period` | `varchar` | Academic term label such as `Fall 2024` | Passthrough from `master_mailing.period` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Human-readable academic term label from the source. |
| `period_sortable` | `varchar` | `YYYY-N`; 1=Winter, 2=Spring, 3=Summer, 4=Fall | Passthrough from `master_mailing.period_sortable` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Sortable academic term code used for chronological ordering. |
| `period_date` | `date` | Canonical date: YYYY-01-01, YYYY-04-01, YYYY-07-01, or YYYY-10-01 | Passthrough from `master_mailing.period_date` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Canonical starting date assigned to the academic term. |
| `instructor` | `varchar` | Instructor-name text; source punctuation and casing retained | Passthrough from `master_mailing.instructor` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Instructor name attached to the course section. |
| `first_name` | `varchar` | Given-name text; source punctuation and casing retained | Passthrough from `master_mailing.first_name` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Given name of the course instructor. |
| `last_name` | `varchar` | Family-name text; source punctuation and casing retained | Passthrough from `master_mailing.last_name` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Family name of the course instructor. |
| `email` | `varchar` | Lowercase, trimmed email text | Passthrough from `master_mailing.email` after membership in `recent_periods` and `NOT EXISTS` exclusion by `opt_out.email`. | Normalized instructor email used for contact and matching. |
| `panel_response_year` | `varchar` | Campaign label shaped `OER_YYYY`, such as `OER_2025` | `panel_email.panel_response_year` LEFT JOINed to `master_mailing` by cleaned email. | Latest recorded panel response campaign label. |
