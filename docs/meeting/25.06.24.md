## Action Items

- [ ]  Add/verify accounts
    - [ ]  Shell login
    - [ ]  Superset
    - [ ]  SQL execution
    - [ ]  SCP for CSV's
- [ ]  Prepare for updated data (schema + new rows, updated rows?)
    - [ ]  Schema changes to existing?
    - [ ]  New rows
    - [ ]  Updated rows?
    - [ ]  New datasets
- [ ]  Superset functionality
    - [ ]  ETL + views
    - [ ]  Dashboard/landing
    - [ ]  Drill-downs (of what?)
    - [ ]  Export
- [ ]  #FIXME
    - [ ]  glen ssh via key/config
    - [ ]  views take a long time to load
    - [ ]  `snake_case` all the tables/cols

![IMG_0142.jpeg](attachment:e7380aa7-6482-4b43-9e80-0719f2219957:IMG_0142.jpeg)

[BW.pdf](attachment:7146afcb-68ef-45ef-a66a-39b8fd4a2eee:BW.pdf)

![IMG_9662.png](attachment:901ed310-d378-4be1-bf66-d756e32b0c2c:IMG_9662.png)

# Connecting to `glen`

With Cyber/MountainDuck: use 1Password shared credentials to copy data by connecting as user “jeff” or “glen”

![Screenshot 2025-08-29 at 5.04.48 PM.png](attachment:895c1639-4dd5-4cf2-8f94-3e1676d3baa8:Screenshot_2025-08-29_at_5.04.48_PM.png)

![Screenshot 2025-08-29 at 5.05.25 PM.png](attachment:68b40c1c-7f25-4059-8442-f499896e0223:Screenshot_2025-08-29_at_5.05.25_PM.png)

# Data Sources

### `course_catalog` - Fka `Bayview.YYYYMMDDD` or `MasterBMG`

- points to `course_catalog_yyyyymmdd` most recent
- Bayview CSV - updates with new rows & cols, dropped ==see excel==
- `email` sourced from scrape, may need validation
    - trim/lower
    - URL encoding issues?
    - comma, multiple emails in single value? take first (*see if this is an actual issue, enough to warrant action?*)

New columns:

- `Book Status` (text): ['recommended', 'required', …]
- `Seats Taken` (numeric)
- `course_id`: `concat(school, deptartment, course number)` (strings, not ID's, plus delimiter '|')
- `section_id`: `course id` with `section` appended

Can drop:

- `Edition`: better from IPEDS
- `PublishedYear`: won't use
- `SchoolYearType`: won't use
- `State`: coalesce from IPEDS
- `Dept Code`: won't use

### `IPEDSData` - Fka `hdic_dist_2022_selected`

- cols from excel
- merges with `course_catalog` by `unit_id`

### `panel` - New

New CSV, ever responded. ==Keep most recent response row==

- `email`: key for merge (trim/lower)

### `opt_out`

- Latest contains all opt-out updates concatenated, no logic required to combine with the past
- `email`: key for merge (trim/lower)

# Unit of Analysis: Email

### `email_issues`

New derived quality check

- `email` (merge key) from `opt_out`, `panel`, `course_catalog` with interior spaces or other issues
- include source table

### `master_mailing`

Derived from `course_catalog`

Load raw → column filter → most recent period per email (limit 1, select first appearance if multiple)

- ~8 cols from excel

### `current_mailing`

Derived from `master_mailing` + `opt_out` + `panel`

- Most recent period in last 12 periods (3y)
- Not `opt_out` flag for removal
- Include `panel` year of last response
- 

### `current_mailing_xx` for `xx` $\in$ State

Derived from `current_mailing`

- State-specific [ca, tx, fl, ny, other], e.g., `current_mailing_ca`

# Unit of Analysis: `course_id`

## `master_section`

Derived from `course_catalog`

- One row per `course_id` x `section_id` x `period`
- Roll-up (wip)
    - `required` x `oer` x `publisher`: count, publisher count, OER status count,

## `master_course`

Derive from `master_section`

- One row per `course_id` x `period`
- Include select cols from `course_catalog`
    - `section_count` (count unique `section`)
    - `enrollment_total` (sum `enrollment`)
    - `seats_taken_total` (sum `seats_taken`)
- Section roll-up logic (well-defined?)
    - Count of number of materials required, optional/supplement
    - Up to `N` required materials colsPublisher information for required materials?
    - Goal to report on publisher, OER,

## `master_course_material`

Derived from `master_course` + ...?

- `publisher_required_count` (count unique `publisher` over `course_id` x `period`)
- `publisher_optional_count`

### Unit of Analysis: `section_id`

From `course_catalog`

- Unique section types somehow by publisher distribution
- Later?
    - Seat count by publisher, OER
    - Dollar count by publish, OER
