# Full pipeline run — 2026-09-08

Status: pipeline and QA complete (started 2026-09-08 18:28:56 PDT; pipeline ended
2026-09-08 20:43:57 PDT; QA completed 2026-09-08 20:47 PDT).

Authorized #26 price-contract rebuild. Metabase was stopped before database writes
(`docker stop metabase`); no other DuckDB/pipeline writer was present.
Supervisor QA accepted; Metabase and Notion publication and verification complete.

## Command and runtime

```text
set -o pipefail; MEM_LIMIT=16GB NUM_THREADS=1 scripts/run_sql.sh 2>&1 | tee output/logs/run-2026-09-08-full.log
```

Configured DB: `duckdb/commodore.duckdb`. Entrypoint selected 40 files: full IMPORT,
EDA, 3 models, and 23 exports. Process wrapper PID 1574434; runner PID 1574455;
initial DuckDB PID 1574530 (finished). Resource preflight: 1.2 TB disk available, 86 GiB RAM
available. Inputs are the configured `2025.12.15` catalog/enrichment files and
`BookPricing.Historical_20260224.csv`; exact sizes/mtimes are in the preflight shell
output and are not duplicated here to avoid exposing source contents.

## Evidence

- Full stdout/stderr log: `output/logs/run-2026-09-08-full.log` (ignored; treat as internal).
- Pre-run output inventory and preserved prior artifacts:
  `output/archive/pre_run_20260908_182824/` (32 GiB).
- Post-run machine-readable QA: `output/logs/qa-2026-09-08-counts.csv`,
  `qa-2026-09-08-grains.csv`, `qa-2026-09-08-price-bounds.csv`,
  `qa-2026-09-08-midranges.csv`; query source is `qa-2026-09-08.sql` and execution
  log is `qa-2026-09-08-exec.log`.

The log is reviewed for PII before forwarding summaries. No publishing, ticket/status,
or commit action is performed by this run owner.

## Baseline targets (September 5)

Source/enriched 102,885,609; `comprehensive_data` 102,885,609;
`course_material` 96,663,781; `master_material` 19,387,814;
`master_section` 10,514,319; `pricing_historical` 27,273,202;
`pricing_wide` 11,917,462; `section_enrollment` 35,232,716;
`master_course` 5,206,159; `sample_material_10pct` 1,937,043
(1,050,536 sections); `sample_section_us_intro_fall2025` 773,613.

Required post-run checks: release exports 37/38/39/41, schema against staged DBML/golden
dictionary, ten monetary columns and required inferred-scope reconciliation at item,
section, course, and sample grains, plus uniqueness/key conservation and relevant tests.

## Results

The real entrypoint exited 0. Rebuilt counts exactly match the September 5 targets:
catalog/comprehensive 102,885,609; course_material 96,663,781; master_material
19,387,814; master_section 10,514,319; pricing_historical 27,273,202; pricing_wide
11,917,462; section_enrollment 35,232,716; master_course 5,206,159;
sample_material_10pct 1,937,043; sample_section_us_intro_fall2025 773,613.
See `qa-2026-09-08-counts.csv` for the exact machine-readable result.

Release 38, 39, and 41 all have zero `is_match=false` rows. Release 37 has 34
declared not-applicable domain rows and every applicable row is within tolerance (no
failed applicable rows). Grain duplicate-key counts are zero for master material,
section, course, and both sample relations. Independent expected→actual checks report
zero mismatches for required/all section bounds and required/all course bounds, and
zero midpoint mismatches for section, course, and section sample. The sample has
1,937,043 rows: 1,600,997 inferred-required and 336,046 inferred-optional.
Edge-case observations are preserved rather than filtered: 78,010 master-material
rows have a genuine zero bound, 1,920,125 have rental terms, and 3 matched rows have
NULL `price_min`.

Schema QA: `information_schema` reports 32 relations and 1,228 columns, matching the
staged DBML contract; the generated golden dictionary has 25 relations / 1,198 fields.
`python3 scripts/generate_data_dictionary.py --check` passed. All 108 pipeline tests,
all 3 Metabase price-summary tests, and the runner's selected-output type validation
passed. No pipeline failure or resolution was required.

Known DQ (not new release regressions): pricing import reports 9,679 byte-identity
duplicates removed and 1,766,922 old snapshots collapsed to the 27,273,202 final rows;
the raw-data checks report 1,336 mixed rental-day consistency cases and 141,394 rows
with UNKNOWN pricing section segments. These are logged by the pipeline's existing DQ
stage and are not reconciliation failures. Resource observation: canonical stage
spilled heavily (disk use peaked around 53%, with >800 GB free); no resource failure.

## Supervisor acceptance

- Independently compared all 1,228 live column names, types, and ordinal positions
  with `schema.dbml`: no missing, extra, or mismatched columns.
- Inspected the full-data QA SQL and its four zero-mismatch bound checks and three
  zero-mismatch midrange checks. Comparisons are NULL-safe and use inferred requiredness.
- Recomputed all ten section monetary fields from `master_material` for a deterministic
  sample of 10,559 sections (`hash(section_id) % 997 = 13`); full-outer key comparison
  and every field matched.
- Rechecked live material/section/course counts: 19,387,814 / 10,514,319 / 5,206,159.
- Checked `price_avg IS DISTINCT FROM (price_min + price_max)/2.0` across every
  `master_material` and `pricing_wide` row: zero mismatches at either item surface.
- Checked the regenerated release files: 240 + 12 + 13 = 265 equality checks pass.
  Sample reconciliation: 254 pass, 34 declared not applicable, zero failures.
- Evidence: `output/logs/qa-2026-09-08-supervisor.json`.
  Item checks: `output/logs/qa-2026-09-08-item-midranges.json`.

Steps 2–4 are accepted. No pipeline rebuild or publication work remains under #26;
PR #62 still requires human review/merge.

## Publication notes

- Metabase still had several older report titles. Each matched a historical repository
  version; scoped updates preserve unrelated settings, IDs, filters, and layouts.
- Independent review caught an incomplete remote-row check in the one-off Notion
  reconciliation. Exact active IDs/keys/values are now checked before writes;
  seven scratch tests cover conflicts, interrupted writes, and resumption.
- Notion accepted all 30 field updates but rejected the legacy `archived` request key
  before trashing any entry. The scratch request now uses the documented
  [`in_trash` field](https://developers.notion.com/reference/patch-page).
  State remained untouched; a fresh preview verifies the acknowledged updates before resuming.
  Original log: `output/logs/notion-price-keys-2026-09-08.log`.
- Resumption succeeded: 30 renamed page IDs retained, exactly three obsolete rows in
  recoverable trash, 1,198 active rows verified before saving state. Both pre-change
  state backups remain under `.notion/`; resumption log:
  `output/logs/notion-price-keys-2026-09-08-resume.log`.
- Material and key reconciliation exports (41 and 39) are byte-identical to the
  preserved pre-run files in `output/archive/pre_run_20260908_182824/output/`.

## Published and verified

Metabase is running and healthy. Schema refresh exposes all ten new monetary fields
and none of the eleven retired fields on section, course, and section sample.
Updated cards: 75, 76, 81, 125, 131, 160; model 158; dashboards 9, 13, 14, 16.
All 74 SQL/filter definitions match the repository; all 14 dashboards preserve their
parameters, layouts, and mappings.

| Changed report | Live result comparison with DuckDB |
|---|---|
| 24 / card 75 | All 2,000 returned rows match; API explicitly truncates 2,232 rows to 2,000. The remaining 232 were not live-API-validated. |
| 25 / card 76 | All 9 rows match. |
| 40 / card 81 | Single row matches. |
| 42 / card 131 | All 11 rows match. |
| 47 / card 160 | All 6 rows match. |

Metabase evidence: `output/logs/metabase-publish-20260908-221331/`, including fresh
pre-write snapshots, published IDs, metadata convergence, query results, and readback checks.
No report SQL or global response limit was changed to accommodate validation.

Notion: all 1,198 field records and 26 downloadable TSVs verified. Four TSVs were
updated (global plus three affected relations); file-block IDs were preserved.
Flow and report pages were published/readback verified; the three detail pages were
unchanged. Repeated field, download, and prose applies made zero remote writes.
Logs: `output/logs/notion-{dictionary,downloads,prose}-2026-09-08-{apply,repeat}.log`.

Local generators, cleanup/mailing checks, 108 pipeline tests, three report tests, and
`git diff --check` pass. No external data files were released. Known #21 join/DQ issues
and #87 non-price master definitions remain outside this completed price-contract batch.
