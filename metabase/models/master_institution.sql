-- name: Master Institution by Term
-- description: One row per period_sortable × unit_id from 2024 onward (#54). The complete section/course/enrollment spine, raw supply-aware required flag, and supply audit are retained; material, inferred-required/optional, OER/IA, pricing, and ISBN measures use the canonical #58 Course Materials Use population. The deterministic bookstore URL is metadata selected from all same-term pricing rows, not a Use metric. NULL unit_id remains one unknown-institution bucket per term so section totals reconcile. Priced-count names follow their actual required/optional status.
SELECT * FROM master_institution
