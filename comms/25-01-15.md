---
notion-id: 23ad9fdd-1a1a-8103-8d59-f414f2c21146
---
There are multiple tasks I need to run on the full database to select various working sub-samples.
Based on what I saw in the previous file, I expect the fields for all records are:
# Columns
```markdown
| Column          | Type    |
| ISBN13          | Real    |
| Title           | Text    |
| Author          | Text    |
| Publisher       | Text    |
| Imprint         | Text    |
| Edition         | Text    |
| Published Year  | Text    |
| Format          | Text    |
| FormatType      | Text    |
| IPEDID          | Integer |
| School          | Text    |
| SchoolYearType  | Text    |
| State           | Text    |
| DeptCode        | Text    |
| Department      | Text    |
| DeptDescription | Text    |
| CourseNumber    | Text    |
| Section         | Integer |
| CourseTitle     | Text    |
| CourseLevel     | Text    |
| CourseSubject   | Text    |
| Period          | Text    |
| Enrollments     | Integer |
| Instructor      | Text    |
| FirstName       | Text    |
| LastName        | Text    |
| E-Mail          | Text    |
| SeatsTaken      | Text    |
| BookStatus      | Text    |
```
# Tasks
1. Create new sortable Period variable with year and term:
- Winter=1
- Spring=2
- Summer=3
- Fall=4

For example: "Fall 2023" would become "2023-4"
2. Left joint to add OptOut code to every record in the database as a new field using the file **Opt_out.csv** (on USB drive) as the lookup table and E-Mail as the index variable.  There will be multiple records with the same email address - each one of them should have the OptOut text string added.  If there is not a match for a particular email address the OptOut field should be left blank.
3. Left join to add 8 selected variables (sector, iclevel, control, instsize, efydetot*tot*22, efyde*tot*22, typeinst, insttype) to every record in the database as a new field using the file **hdic_dist_2022_selected.csv** (on USB drive) as the lookup table and IPEDID as the index variable.  There will be multiple records with the same IPEDID - each one of them should have the 8 selected variables added.  If there is not a match for a particular IPEDID the 8 selected variables fields should be left blank.
4. Create a new database with a subset of records selected from the master database:

```sql
    Select if "Period" IN ("Winter 2024" \
                      , "Spring 2024" \
                      , "Summer 2024" \
                      , "Fall 2024")
    
    AND
    
    E-Mail is not blank
    
    AND
    
    OptOut is blank
```
Save the resulting database of "Current Working"
5. Use the "Current Working" database and select all records containing a unique E-Mail address.  The order for selection for all duplicate E-Mail is by

Period: “Fall 2024" then “Summer 2024" then “Spring 2024" then “Winter 2024"
Enrollment: Largest to smallest
If there are duplicates still remaining, then select which ever one is first appearing.
Save the resulting database as "Current E-Mail"
6. Divide the "Current E-Mail" file into two files:

"Current OpenStax Email": All with the text "OpenStax" included in any position in the Publisher field.
"Current General Email": All where the text "OpenStax" in not included in any position in the Publisher field.
Save both files.
7. Select all records from the master database where the state is Texas (TX).

Save the resulting database as “Texas Full”
8. I’d like to use one of those determine the gender by first name services to add the gender (along with the probability) - not sure which one yet.
9. I’d also like to build a master list of all previous sample (indexed by email address) that I could use to know which projects any email address has been sued for, and the status of that (bounced, sent, opened, started, finished).  This will take some time for me to build, but will eventually be a flat file CSV to left join the main database.